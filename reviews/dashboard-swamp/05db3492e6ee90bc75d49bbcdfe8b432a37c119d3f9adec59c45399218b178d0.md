# Secrets & Security Review
extension: @jpisgeek/dashboard-swamp            version: 2026.09.05.1
content-hash: 05db3492e6ee90bc75d49bbcdfe8b432a37c119d3f9adec59c45399218b178d0
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all five published files; no credential, private operator path/host/account, control character, or hidden infrastructure identifier was found. Public publisher/repository/vendor identity and synthetic examples are accepted.
2. Credential handling: this local-only extension holds no credential; removed remote URL/token arguments are detected before schema stripping and refused before any command. The child receives an isolated HOME and a cleared, minimal environment, and raw success/failure output is never logged or persisted.
3. Transport and network: no network request or remote mode exists. The four observations invoke a resolved local Swamp executable only; the unsupported heartbeat is recorded without probing an internal API. Caller cancellation and timeouts terminate subprocesses and remain distinct from observed failures.
4. Untrusted input: fixed argv avoids shell/argument injection; executable and PATH resolution reject relative/repository-contained targets; stdout/stderr, response arrays, stored handles and individual records are bounded before expensive work; strict per-interface projections retain only counts, booleans and a fixed status vocabulary; duplicate/mismatched/unreadable snapshots become coverage gaps. The 16-handle cap is applied before repository reads and omitted handles produce a required partial section with explicit observed/expected counts and a warning exception. The runner override is per invocation and cannot contaminate later observations.
5. Supply chain: only sanctioned runtime `npm:zod@4` and built-in `node:crypto` are imported; no dynamic code, eval, shell, fetched executable, or third-party bundled dependency exists. The configured local executable is resolved to a real path outside the observed repository before launch.
6. Documentation matches code: verified arguments, one collector method, observation resource, report, four CLI queries, unsupported heartbeat, local-only boundary, subprocess isolation/bounds, five-minute freshness, duplicate/invalid behavior, 16-handle partial-coverage behavior, namespaced identities and written-data disclosure.
7. Data written: see below. Stored observations and report output use projected operational counts/statuses and hashed namespaces; source model names/IDs, report names, paths, identifiers and raw response text are not written by the extension.

## Data written by this extension
- Thirty-day `observation` resources for run history, run doctor, workflow history and stored reports: interface name, availability, timestamp, fixed-class error, and projected counts/booleans/status buckets. An explicit unsupported heartbeat observation is also written.
- A dashboard bundle report: required operational sections, freshness/completeness/coverage, projected metrics/facts and fixed exceptions. Bundle, section and exception identities contain a SHA-256 namespace derived from the source model identity without emitting that identity.

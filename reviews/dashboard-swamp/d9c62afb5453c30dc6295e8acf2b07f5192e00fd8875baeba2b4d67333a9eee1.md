# Secrets & Security Review
extension: dashboard-swamp            version: 2026.08.25.2
content-hash: d9c62afb5453c30dc6295e8acf2b07f5192e00fd8875baeba2b4d67333a9eee1
reviewer: GPT-5 Codex   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | swamp_observability.ts:16-29,151-163 | A serve token can be directed to any HTTPS hostname. HTTPS alone does not prevent credential exfiltration through a typo or hostile configuration. | `server: z.string().url()` and `env: g.token ? { SWAMP_SERVER_TOKEN: g.token } : {}` | Bind credentials to an explicit, independently configured hostname allowlist; canonicalize and validate the exact host before launching the command. |
| 2 | block | swamp_observability.ts:43-58,180-188,242-255 | Successful command responses are neither strictly validated nor redacted before being persisted for 30 days. Arbitrary response fields, including secrets or identifying metadata, can be written to `.swamp/data`. | `payload: z.json().nullable()` and `payload: JSON.parse(result.stdout)` | Define strict per-interface response schemas, reject unknown fields, and persist only a safe projected representation after explicit secret and personal-data filtering. |
| 3 | block | swamp_observability.ts:13-15,76-85,159-163 | The configurable executable inherits the entire parent environment in addition to the serve token. A replaced, mistaken, or malicious executable can read unrelated credentials. | `clearEnv: false` | Use `clearEnv: true`, pass only a minimal allowlist of required variables, and validate or resolve the expected Swamp executable safely. |
| 4 | block | swamp_observability.ts:76-96 | An already-aborted caller signal is not honored before spawning. The process starts first, and adding an abort listener to an already-aborted signal does not invoke it. | `const child = command.spawn()` followed by `options.signal.addEventListener("abort", abort, { once: true })` | Call `throwIfAborted()` before spawning and guarantee process termination for later cancellation, including a bounded forced-kill fallback. |
| 5 | block | dashboard_swamp.ts:947-965 | Run-doctor counts accept any JavaScript number rather than nonnegative integers. Negative values can pass completeness checks and produce a healthy state. | `typeof payload[key] === "number"` and `(stale ?? 0) + (orphaned ?? 0) > 0` | Parse the complete response with a strict schema using finite, nonnegative integers; treat invalid counts as an invalid response or explicit coverage gap. |
| 6 | fix | dashboard_swamp.ts:438-442,1138-1148; README.md:40-44 | The report JSON writes the local model name and model ID while declaring no sensitive fields, and the README does not disclose those identifiers in its data-written section. | `modelName: ctx.definition.name`, `modelId: ctx.modelId`, and `fields: []` | Omit or pseudonymize the identifiers, or classify them as sensitive and document their storage and exposure in the README. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Static published surface: checked credentials, identities, infrastructure names, domains, addresses, local paths, secret-store names, and control characters; no unaccepted static disclosure found.
2. Credential handling: see findings 2 and 3; the declared token itself is correctly marked sensitive and excluded from argv and sanitized failure text.
3. Transport and network: see findings 1 and 4; cleartext configured URLs and URL userinfo are rejected, and no TLS-verification-disable option exists.
4. Untrusted input: see findings 2 and 5; no HTML, shell-command concatenation, SQL construction, or resource-name collision was found.
5. Supply chain: only the sanctioned `npm:zod@4` runtime import is present; no undeclared dependency, dynamic remote import, fetched execution, `eval`, or `new Function` was found.
6. Documentation matches code: see finding 6; method count, arguments, resource name, instance names, failure behavior, and example configuration otherwise match.
7. Data written: see findings 2 and 6.

## Data written by this extension
- `observation` resources, retained for 30 days: interface name, availability, timestamp, sanitized error classification/text, and verbatim successful CLI JSON. The payload may contain run, workflow, report, model, repository, path, user-controlled, personal, or secret-bearing fields because it is unrestricted.
- Stored report JSON: aggregate dashboard state, coverage, freshness, counts, exceptions, extension metadata, local model name, and model ID.
- Stored report Markdown: dashboard title plus section states and generated summaries; no credential field is intentionally included.
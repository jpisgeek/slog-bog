# Secrets & Security Review
extension: @jpisgeek/dashboard-swamp            version: 2026.09.05.1
content-hash: 900b939c4eb7f41bd08c3e1d94b860f9a705b31fbcf017687a6bd918268f16f7
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | `dashboard-swamp/swamp_observability.ts:629` | The published module exposes a process-global setter that lets any importer replace the command runner used by later legitimate observations. The replacement can fabricate successful command output, turning failed or absent Swamp interfaces into healthy stored observations and contradicting the documented fixed-argv production boundary. | `export function setCommandRunnerForTest` assigns caller code to module-global `commandRunner`, and `query()` invokes that mutable global at line 757. | Remove the exported mutable setter from published source. Inject a runner only through a per-call context seam held by the caller executing that same observation, defaulting directly to `defaultRunner`, so one importer cannot alter another run. Add a regression proving separate calls cannot inherit a prior runner override. |
| 2 | block | `dashboard-swamp/dashboard_swamp.ts:1286` | The report has no bound on execution-scoped data handles. It reads and parses every matching handle, up to 4 MiB each, before duplicate-interface detection, so a forged or stale handle population can impose unbounded repository reads, decode/parse work, and memory churn. | `for (const handle of ctx.dataHandles)` has no prefix cap; the per-record byte cap at line 1298 does not bound total work. | Cap handles before any repository read, preserve a small bounded prefix, and emit an explicit critical or partial coverage condition when the count exceeds the supported maximum. Avoid silently slicing without reporting omitted coverage. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all five published files. Found no credential, private infrastructure literal, real local path, private domain, account ID, secret-store item, control character, or hidden secret. Publisher/repository identities and Swamp names are accepted; paths and model names in examples are explicit placeholders.
2. Credential handling: the model declares no credential and refuses removed remote server/token arguments before schema stripping. The child receives a cleared environment with a private empty HOME and only sanitized PATH/TMPDIR; command output, response identifiers, server text, repository errors, and stored failure text are not persisted or rendered. Swamp method summaries still record operator-configured repo and executable paths as documented operational metadata.
3. Transport and network: the extension opens no network connection and exposes no remote URL or token. The local child command uses fixed argv and no shell in the default runner. Redirect and TLS surfaces were removed with remote observation. Mutable runner boundary: see finding 1.
4. Untrusted input: successful command responses are projected through strict per-interface schemas to bounded counts, booleans, and a closed status vocabulary; unknown status text is discarded. Output pipes have hard byte caps, invalid/oversized/missing/duplicate observations remain coverage gaps, stale and future timestamps cannot claim freshness, and stored error prose is never rendered. Bundle/section/exception identities use a per-model full SHA-256 namespace. Total handle bound: see finding 2.
5. Supply chain: the only imports are sanctioned runtime `npm:zod@4` and built-in `node:crypto`; there are no remote dynamic imports, fetched code, evaluation, or deserialization into code. The inlined dashboard contract is byte-identical to the reviewed canonical copy.
6. Documentation matches code: verified model/report types and version, arguments, single `observe` method, five interfaces, fixed local command argv, removed remote mode, output/environment limits, strict projection, 30-day resources, report freshness and coverage semantics, identity handling, source-only installation, and data inventory. The global runner setter and unbounded report handle walk are not disclosed; see findings 1 and 2.
7. Data written: see below. The observation projection removes response identifiers and free text, while model summaries outside these resources can retain configured `repoDir` and `swampBinary` as documented operational metadata. No credential is written.

## Data written by this extension
- Five `observation` resources retained for 30 days: interface name, availability, observation time, closed error classification and fixed message, plus strictly projected run/workflow/report status records or run-doctor counts. Raw command output, record identifiers, names, paths, and remote text are omitted; the heartbeat interface is explicitly unsupported.
- Report JSON: a dashboard bundle with five interface sections, projected counts and status buckets, freshness/completeness/coverage exceptions, and full SHA-256 namespaced identities. Producer model name and raw model ID are omitted.
- Report Markdown: fixed bundle title/state and normalized interface section state/summary lines. It contains no command output, credential, configured repository path, executable path, source identifier, or stored error text.

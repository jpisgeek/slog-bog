# Secrets & Security Review
extension: @jpisgeek/dashboard            version: 2026.09.05.1
content-hash: 6afeb96233798d0d0d0438a8e350deb4556f09f1aa3430bd65ac222672378055
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | operator-decision | `dashboard/dashboard.ts:91` | The inlined bundle contract deliberately uses `.passthrough()` throughout, contrary to the gate's strict-response default. Unknown v1 fields survive parsing, increasing retained in-memory surface, although the renderer's explicit input budget limits size and only named fields are rendered or written. | `Unknown additive v1 fields survive parsing through passthrough schemas.` | Operator accepts forward-compatible additive v1 data or changes the schemas to default strip/strict behavior. If retained, keep the current budget and named-field rendering boundary documented and tested. |
| 2 | operator-decision | `dashboard/dashboard.ts:1177` | The model writes and overwrites any operator-configured filesystem path reachable by the Swamp process. A mistaken model configuration can replace an unrelated file; the adapter supplies no workspace-root or output-directory boundary. | `await Deno.writeTextFile(g.outputPath, html);` and the README states that the file is overwritten at the operator-configured path. | Operator accepts unrestricted deployment paths or introduces a canonical output root and requires the resolved target to remain beneath it, with symlink/ancestor checks and atomic replacement. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all four published files, including source comments, literals, README examples, manifest, and license. Found no credential, private infrastructure identifier, real local path, private host/domain, account ID, secret-store item, control character, or hidden secret. The license identity, public package scope, repository, and documentation-only address `192.0.2.10` are accepted public or reserved-example identities.
2. Credential handling: the extension has no credential argument, vault access, environment access, subprocess, or network authentication. Evidence URL userinfo is rejected. Bundle values can contain operational or sensitive dashboard data by design, but no schema field is designated for credentials and the README tells operators to protect rendered dashboards according to their contents.
3. Transport and network: no network request is made. HTTPS-only, credential-free evidence URLs are validated as data and are not fetched or rendered into links. There is no TLS-disable, redirect, cleartext transport, file URL, or network timeout surface.
4. Untrusted input: duplicate section IDs and duplicate exception IDs across bundle and section scope reject the bundle and produce a critical coverage exception. Arrays are length-checked before traversal; object values are visited through a bounded own-key walk without allocating an entries array. Prior cleanup metadata must pass the strict current render schema; cleanup targets are deduplicated and restricted to the exact current `exception-<slug>-<64 lowercase hex>` resource-name form, while invalid metadata skips deletion and emits a warning. Bundle-derived strings reaching HTML are escaped, synthetic identities use length-delimited tuples and full SHA-256, and invalid or incomplete inputs cannot become healthy state.
5. Supply chain: the only external import is sanctioned runtime `npm:zod@4`; `node:crypto` is built in. No remote dynamic import, undeclared dependency, load-time fetch, `eval`, `new Function`, or untrusted deserialization into code was found.
6. Documentation matches code: verified type and version, four global arguments, one `render` method, two resource kinds, bundle-v1 limitation, exact input and output bounds, duplicate-identity rejection, cleanup validation and warning behavior, HTML escaping, output overwrite behavior, source-only installation, and no-network behavior against the implementation. See findings 1 and 2 for documented design choices.
7. Data written: see below. Potentially identifying operational input is disclosed in the README Security section, sensitivity labels are retained, and no credential-specific field exists.

## Data written by this extension
- `exception` resources store normalized producer or coverage condition `id`, severity, subject, headline, detail, source, sensitivity, suppression state/reason, and truncation state. Subject, detail, source, and suppression reason can contain personal, infrastructure, or sensitive operational text supplied by a producer or operator; they are HTML-escaped but not redacted.
- The `render` resource stores the configured output path, HTML byte count, exception/severity counts, bundle counts and IDs, coverage states, validated current exception resource names, and render timestamp. Output paths and bundle IDs can identify local layout or internal systems.
- The self-contained HTML file contains the configured title, validated bundle titles and states, section summaries and coverage reasons, metric labels/values/limits, fact labels/values, exceptions, sources, suppression reasons, and timestamps. Sensitivity metadata is advisory and does not remove or access-control this content.

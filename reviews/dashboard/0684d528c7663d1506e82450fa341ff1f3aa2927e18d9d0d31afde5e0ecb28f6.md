```
# Secrets & Security Review
extension: dashboard            version: 2026.08.22.2
content-hash: 0684d528c7663d1506e82450fa341ff1f3aa2927e18d9d0d31afde5e0ecb28f6
reviewer: claude-fable-5      date: 2026-08-23
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | fix | dashboard/dashboard.ts:44 (README.md arguments table) | `certWarnDays` is declared, defaulted, and documented in the README, but never read anywhere in `render.execute`. The cert rules use the truenas model's `expired`/`expiringSoon` flags and a hard-coded `days <= 7`. A user who copies the README example and sets `certWarnDays: 60` gets no change in behaviour and believes a 60-day warning is configured. Documentation/code drift that masks a non-working control. | `certWarnDays: z.number().int().positive().default(30),` — no other occurrence of `certWarnDays` in the file | Either wire it into the cert evaluation (`days <= g.certWarnDays` → warning) or remove the argument from the schema so the README stops advertising it. |
| 2 | fix | dashboard/dashboard.ts:431 | The CertificateIsExpiring dedupe finds the *first* `cert:` exception regardless of which certificate the alert concerns, then `continue`s. If any cert exception exists, every TrueNAS cert alert — including one for a different cert that the cert loop did not flag (e.g. `expiryKnown` false) — is silently swallowed and its "dismissed" note lands on the wrong row. Failure hidden as health. | `const certExc = exceptions.find((e) => e.id.startsWith("cert:"));` | Match the alert to its certificate (by name/id carried in the alert record) and only fold when the ids agree; otherwise fall through and raise the alert. |
| 3 | fix | dashboard/dashboard.ts:364, :719 | Missing `usedPercent` is backfilled with `0`, so a mount record lacking the field renders as 0 % used and never raises the disk rule. Absent reads as fine. | `const pct = Number(m.usedPercent ?? 0);` | Treat missing/NaN as unknown: skip the threshold, render `?`, and surface it as an info exception or in the stale banner. |
| 4 | fix | dashboard/dashboard.ts:397, :732 | A cert record with `expiryKnown` missing (schema drift upstream) is skipped by the evaluator and rendered as "no expiry", which reads as benign. Same absent-reads-as-fine pattern as #3. | `if (!c.expiryKnown) continue;` / `c.expiryKnown ? … : "<em>no expiry</em>"` | Distinguish `expiryKnown === false` (genuinely no expiry) from `undefined` (unknown); render unknown as `?` and do not skip silently. |
| 5 | fix | dashboard/dashboard.ts:280–322 | A read error mid-loop (after ≥1 record) leaves partial `rows`, so the source is *not* marked stale (`rows.length === 0` is the only stale test); the page shows a partial view of that source with no on-page indication, while `sourcesRead` is not incremented. Partial failure presented as a complete picture. | `} catch (e) { errored = true; … } if (rows.length === 0) { stale.push(src.name);` | Push to `stale` (or a separate `partial` list shown on the page) whenever `errored` is true, regardless of row count. |
| 6 | fix | dashboard/dashboard.ts:650–660 | Pruning deletes every prior `exception-*` resource not regenerated this run. When a source is unreadable (transient repo error) it contributes zero exceptions, so all of its previously-written exception resources — the records the README says alerting should be built on — are deleted; an alert consumer sees them vanish as if resolved. The page discloses staleness, the resource store does not. | `if (rec.name.startsWith("exception-") && !liveNames.has(rec.name)) { await ctx.deleteResource(rec.name);` | Skip pruning of exceptions whose `source` belongs to a stale/errored source this run, or carry them forward marked stale. |
| 7 | fix | dashboard/dashboard.ts:337, :352, :367, :452, :506 | Exception ids are `:`-joined from data fields and `:` can occur in the inputs (hostnames with ports, IPv6 literals, alarm names). `alarm:a:b:c` is ambiguous; two different (node,name) pairs can produce the same id, the same resource name (hash is over the identical id string), and the same suppression match. The FNV hash only protects slug collisions, not id collisions. | `id: \`alarm:${a.node}:${a.name}\`` | Encode components (e.g. percent-encode `:` / use JSON array as the hashed key) or include the source record `__name` in the id. |
| 8 | fix | dashboard/dashboard.ts:739 | Blind cast of an untyped field: `networks` is assumed to be `string[]`. A string or object value throws on `.join` and aborts the whole render (after resources are written, before HTML/pruning). | `esc((m.networks as string[] ?? []).join(", "))` | `Array.isArray(m.networks) ? m.networks.map(String).join(", ") : esc(m.networks)`. |
| 9 | note | dashboard/dashboard.ts:356, :371, :388, :405 | Missing numeric/text fields interpolate as literal `undefined` / `NaN` in headlines and details (e.g. `"undefined — undefinedundefined"`). Fail-loud, not masked, but noisy and written into `exception` resources. | `detail: \`${a.name} — ${a.value}${a.units}\`` | Guard with `?? "?"` as done for the facts row. |
| 10 | note | dashboard/dashboard.ts:342 vs :357, :372 | Unreachable-node exceptions carry `source: "telemetry"` while alarm/mount exceptions from the same collector carry `source: "netdata"`. Querying exceptions by `source` splits one collector across two values; README does not enumerate `source` values. | `source: "telemetry",` / `source: "netdata",` | Pick one value per collector and document the set in the README. |
| 11 | note | dashboard/dashboard.ts:641, :625–628 | `outputPath` is an unvalidated path written with `Deno.writeTextFile` (and `mkdir -p` of its parent). Operator-controlled config, so not an injection, but any path the swamp process can write is overwritten without confirmation. | `await Deno.writeTextFile(out, html);` | Document that the path is overwritten; optionally refuse paths outside the repo unless absolute is explicitly intended. |
| 12 | note | dashboard/README.md "Security" | Disclosure is incomplete: the page and the `exception` resources also publish OS name/version, Netdata version, transport, cloud-claim state, device type and network membership, cert common names, the last line of SSH `stderr`, and TrueNAS alert text (up to 160 chars, which can contain hostnames or e-mail addresses). README lists "machine names, addresses, mounts, and certificate names". | "The rendered page lists machine names, addresses, mounts, and certificate names from your collectors" | Extend the sentence to cover software versions, SSH stderr, and TrueNAS alert text, and state that `exception` resources carry the same fields. |
| 13 | note | dashboard/LICENSE:3 | Copyright line carries the operator's real name alongside the `@jpisgeek` handle used throughout. Conventional for MIT and presumably intentional, but it is identifying information on the published surface. | `Copyright (c) 2026 Jason Powell` | No change required if intended; otherwise publish under the handle. |
| 14 | note | dashboard/dashboard.ts:745, banner | When every source is stale the banner still reads "Nothing needs you / All checks passed at last collection" in the `clear` style; the stale warning appears beneath it. Disclosed, but the headline is false when nothing was collected. | `allClear ? "All checks passed at last collection."` | Gate `allClear` on `stale.length === 0`, or change the sub-line when sources are stale. |

## Checked and clean
1. Secrets/identifiers: all three published files plus manifest read in full. No credentials, keys, tokens, hostnames, IPs, MAC/serials, e-mail, secret-store paths, or local filesystem paths. Placeholders are `host.example.net`, `printer`, `<id from ...>`, `<your-…>`. Only item is the LICENSE name (#13). Visual scan found no control characters; raw-byte check not possible from this input — rely on the CI identifier scanner for NUL/control bytes.
2. Credential handling: no argument carries a secret; no vault access; nothing secret logged, thrown, tagged, or persisted. Log lines carry model type/id and repo error messages only. Clean.
3. Transport/network: no network calls of any kind (no `fetch`, sockets, or URLs beyond the repo link). Only disk I/O is the output file (#11). Clean.
4. Untrusted input: every data interpolation in `renderHtml` passes through `esc()` (text contexts only; `class`/`style` values are code-controlled or numeric) — no HTML injection path found. No shell/SQL/SSH execution. Cross-model data is unschemaed `Record<string, unknown>` — see findings #2–#8 for the absent-reads-as-fine and naming cases; fail-loud cases (`!n.reachable`, `!p.healthy`, missing `exitCode`) are handled correctly.
5. Supply chain: single import `npm:zod@4` (sanctioned runtime-provided major). No other imports, no dynamic `import()`, no `eval`/`new Function`, no remote code. Clean.
6. README vs code: argument names/defaults, method name/count, resource names and field lists, check name and skip behaviour, instance-name format (`unreachable:<name>`), and example config all match — except the dead `certWarnDays` argument (#1), the undocumented hard-coded 95 % critical disk threshold, the `source` value inconsistency (#10), and the incomplete data-disclosure sentence (#12).
7. Data written: see below; personal/identifying fields are present and the README discloses most of them (#12 for the gap). No write can include a secret.

## Data written by this extension
- `exception` resource (≤ one per detected exception, pruned when resolved): `id`, `severity`, `subject` (hostname / node name / "nas"), `headline` (mount paths, cert names, alarm text, TrueNAS alert text ≤160 chars), `detail` (cert common names, last line of SSH `stderr` ≤140 chars, alarm value+units), `source`, `suppressed`, `suppressReason` (operator free text), `truncated`; tags `severity`, `subject`, `suppressed`. Personal/identifying: hostnames, cert CNs, SSH stderr, TrueNAS alert text.
- `render` resource (single instance `render`): `outputPath` (local filesystem path from config), `bytes`, counts, `sourcesStale` (alias list), `renderedAt`. Identifying: output path.
- HTML file at `outputPath` (overwritten each run): everything above plus node OS name/version, Netdata version, transport, reachability, cloud-claim flag; mount node/path/size/used %; cert name/days/CN; Firewalla machine name, primary IP, device type, networks, online state; device/machine/pool/disk counts. Identifying: hostnames, IPs, software versions, network topology.
```

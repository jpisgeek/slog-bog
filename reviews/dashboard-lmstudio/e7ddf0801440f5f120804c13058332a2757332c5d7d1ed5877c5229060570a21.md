# Secrets & Security Review
extension: @jpisgeek/dashboard-lmstudio            version: 2026.09.05.1
content-hash: e7ddf0801440f5f120804c13058332a2757332c5d7d1ed5877c5229060570a21
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all four published files. Found no credential, private infrastructure literal, real local path, private domain, account ID, secret-store item, control character, or hidden secret. Publisher/repository identities and LM Studio names are accepted; examples are explicit placeholders.
2. Credential handling: the report accepts no credential. Raw collector diagnostic prose and repository/parser errors are discarded; endpoint strings are control/invisible-character screened, bounded, and redacted for credential, URL, address, hostname, mail, and filesystem-path shapes before persistence. Raw producer model name and ID are omitted, and sensitivity metadata records omissions and transformations.
3. Transport and network: the report makes no network request and exposes no transport, redirect, TLS, or URL-fetch surface. It reads only execution-scoped Swamp data through the report context.
4. Untrusted input: source records over 1 MiB are rejected before decode. Every method record has a strict schema with cross-field invariants for HTTP success, authorization, daemon state, loaded-model counts, capability evidence, completion usage, and reasoning/output conditions. Invalid, oversized, failed, missing, unauthorized, truncated, and unmeasured observations remain explicit. Lists are capped with visible partial completeness. Length-prefixed SHA-256 identities separate source type, instance, method, family, and probed model; unknown error kinds are hashed rather than interpolated. Markdown escaping is context appropriate. Nothing reaches HTML, shell, SQL, or filesystem paths.
5. Supply chain: imports are sanctioned runtime `npm:zod@4` and built-in `node:crypto`; there are no dynamic imports, fetched code, evaluation, or deserialization into code. The inlined dashboard contract is byte-identical to the reviewed canonical copy.
6. Documentation matches code: verified all six supported source/method pairs, scoped-read behavior, 1 MiB cap, list truncation, strict contradiction rejection, completion accounting scope, context/output heuristics, unknown reasoning metrics, string screening, omitted producer identity, examples, source-only installation, and no-network claim.
7. Data written: see below. Model identifiers, loaded-model inventory, HTTP status, capability findings, token counts, finish reason, and timings are operational metadata; the README warns users that screening reduces accidental disclosure but is not publication authorization. No credential or raw diagnostic prose is written.

## Data written by this extension
- Report JSON: one dashboard bundle for endpoint health/models, daemon state, embedding/completion probes, or capability checks. It contains normalized availability/state, bounded and screened model or finish-reason text, operational counts/timings/status/capability facts, explicit coverage exceptions, and an opaque per-source identity. Raw producer model name/ID and raw error prose are omitted.
- Report Markdown: fixed bundle title/state plus the single screened section title and summary. Markdown metacharacters and line breaks are escaped and no raw diagnostic or credential field is rendered.

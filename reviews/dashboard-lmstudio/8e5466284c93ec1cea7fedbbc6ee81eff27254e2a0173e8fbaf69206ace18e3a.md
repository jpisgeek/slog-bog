# Secrets & Security Review
extension: dashboard-lmstudio            version: 2026.08.25.2
content-hash: 8e5466284c93ec1cea7fedbbc6ee81eff27254e2a0173e8fbaf69206ace18e3a
reviewer: claude-fable-5      date: 2026-08-25
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | fix | dashboard-lmstudio/dashboard_lmstudio.ts:1327 | Collector-controlled text reaches the rendered markdown artifact unescaped: `markdown()` interpolates `section.summary`, which for daemon (857), embedding (936), and completion (1051) is `value.error \|\| …`, and success summaries interpolate `value.model`. A hostile or compromised endpoint that influences upstream error/model strings can inject markdown (or raw HTML, if a downstream viewer allows it) into dashboards. | `` `- ${section.title}: ${section.summary}` `` fed by `const summary = value.error \|\|` | Escape or strip markdown/HTML metacharacters before interpolation, or restrict markdown-artifact summaries to the fixed classified strings and keep raw error text only in structured JSON fields. |
| 2 | note | dashboard-lmstudio/dashboard_lmstudio.ts:763 | Exception details and summaries republish collector `error` strings verbatim (763, 873, 991, 1035). The README's guarantees — token/prompt absent, hostname sanitized, no userinfo/query — are properties of the upstream `@jpisgeek/lmstudio` collector that this adapter does not enforce or re-check; if the collector ever regresses, this extension republishes the leak into stored reports. README.md:47–51 documents the reliance. | `` value.error \|\| `health probe reported ${kind}` `` | None required in this extension; keep the sanitization guarantee tested in the collector's own release gate. Optionally length-cap and character-filter `error` here as defense in depth. |
| 3 | note | dashboard-lmstudio/dashboard_lmstudio.ts:1293 | Only `ZodError` is downgraded to the invalid-record section; if the data handle contains malformed JSON, `JSON.parse` (1234) throws `SyntaxError`, which is rethrown. The failure is loud (report execution fails — never disguised as health), but the documented "invalid record stays distinct" handling does not cover non-JSON content, and V8's parse error message can quote a fragment of the record into the runtime error channel. | `if (!(error instanceof z.ZodError)) throw error;` | Catch `SyntaxError` from `readRecord` and route it to `invalidRecordSection` as well. |
| 4 | note | dashboard-lmstudio/README.md:23 | Doc drift: the example comment says "either @jpisgeek/lmstudio model definition" (implying two), but the code accepts three source types — endpoint, probe, and daemon (dashboard_lmstudio.ts:1243–1246). The example itself is copy-safe. | `# Require this report on either @jpisgeek/lmstudio model definition.` | Regenerate the README with "any @jpisgeek/lmstudio model definition" or enumerate all three. |
| 5 | note | dashboard-lmstudio/dashboard_lmstudio.ts:1304 | `extensionVersion` is hard-coded in the producer block. It matches manifest `2026.08.25.2` today, but a future manifest bump without touching this line silently misattributes provenance in every written bundle. | `extensionVersion: "2026.08.25.2",` | Inject the version from the manifest at build/generation time, or add a release-gate check that the two match. |
| 6 | note | dashboard-lmstudio/LICENSE:3 | The operator's real name appears in the MIT copyright line. This is standard, intentional open-source attribution consistent with the already-public `jpisgeek` GitHub identity — flagged only so the name publication is an explicit choice, not an oversight. | `Copyright (c) 2026 Jason Powell` | None needed; substitute the handle if name publication is unwanted. |

## Checked and clean
1. Secrets/identifying info: swept code, comments, string literals, README prose, and manifest — no credentials, tokens, keys, PEM blocks, or cookies; no hostnames, IPs (RFC 1918 or public), personal domains, device names, or email addresses; no secret-store paths or `vault.get` calls; no local filesystem paths; grep for control characters/NUL across all four files found none. Only identifying items are findings 2 (runtime hostname retention, documented) and 6 (intentional LICENSE name); the `github.com/jpisgeek/slog-bog` URL is the deliberate public source link.
2. Credential handling: the report accepts no secret-bearing arguments (`methodArgs` is declared but never read or emitted); no secrets logged, thrown, persisted, or placed in names/URLs; error paths republish only collector-classified text — see findings 1–2.
3. Transport/network: no network code exists — no fetch, no URLs constructed, no TLS options, no timeouts needed; README's "no network, process, environment, or vault operations" claim matches the code exactly.
4. Untrusted input: all six collector record shapes are validated with strict Zod object schemas plus cross-field `superRefine` consistency checks (count-vs-list, token sums, mutually exclusive flags); unavailable metrics are structurally distinct from zero; every failure path maps to critical/degraded/partial/unauthorized/unsupported, never healthy; section/bundle IDs are fixed strings or platform method names re-validated by `IdentifierSchema`. Residual gaps are findings 1 (markdown interpolation) and 3 (non-JSON records). The `.passthrough()` calls are on the bundle *output* contract over locally constructed objects, not on untrusted input, and are documented v1-evolution behavior — not a finding.
5. Supply chain: exactly one import, `npm:zod@4`, which is the sanctioned platform-provided exception; no other npm/jsr/https imports, no dynamic `import()`, no eval/`new Function`, manifest `dependencies: []`.
6. Docs vs code: extension name, version, install name, `dataName` (`report-jpisgeek-dashboard-lmstudio-json`), the six distinct failure classes claimed in the README opening, `accountingScope`/`aggregateAccounting` values, the contextExhausted heuristic caveat, and the security claims were each verified against the source; drift is limited to findings 4–5.
7. Data written: enumerated below; hostname-retention and report-protection guidance is present in README.md:49–51.

## Data written by this extension
- One report artifact per execution (`report-jpisgeek-dashboard-lmstudio-json` JSON bundle plus a markdown rendering) containing: model identifiers (available-model list and loaded-model inventory — mildly fingerprints the operator's local model library; documented as operational metadata), HTTP status codes, latency values, token usage counts, finish reasons, capability flags, ISO timestamps, and the swamp model name/ID in the producer block.
- Collector-supplied error strings inside exception details and summaries, which per the upstream contract may retain a sanitized endpoint hostname or URL (no userinfo, no query) — the README instructs the user to protect stored reports and rendered dashboards accordingly.
- No credentials, prompts, user lists, MAC addresses, or serial numbers are written.

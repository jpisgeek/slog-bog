# Secrets & Security Review
extension: dashboard-lmstudio            version: 2026.08.25.3
content-hash: 2c6a32407643b792e3e630b23308ca1868d89d4135c5a86ee93878ab83868155
reviewer: GPT-5 Codex   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | dashboard-lmstudio/dashboard_lmstudio.ts:755-762,848-870,981-984,1080-1082,1148-1151; dashboard-lmstudio/README.md:52-58 | Untrusted collector errors are length-clamped but not sanitized before entering JSON, Markdown summaries, and exception details. They can retain credentials, URL userinfo/query strings, hostnames, filesystem paths, or control characters. The output is incorrectly classified as operational and unredacted. The README’s sanitization claim is therefore unsupported. | `const errorDetail = clampText(value.error, MAX_FREE_TEXT);` and `redacted: false` | Apply structured redaction and control-character removal before persistence. Strip credentials, userinfo, queries, private infrastructure identifiers, and filesystem paths; classify or reject anything that cannot be made safe. |
| 2 | block | dashboard-lmstudio/README.md:29-32 | The example publishes `lmstudio-probe` as a model-definition name without marking it as fake. Private model/workflow names are prohibited, and every placeholder must be unmistakably synthetic. The example also fails unless the user happens to use that exact name. | `data.latest("lmstudio-probe", ...)` | Use an explicit placeholder such as `<your-lmstudio-model-name>` and show where that name is defined. |
| 3 | block | dashboard-lmstudio/dashboard_lmstudio.ts:456-574,848-860,968-984,1071-1089,1144-1156 | Response schemas allow contradictory success indicators, and health derivation ignores HTTP status and related flags. A 500 response with an empty `errorKind` can be reported healthy; a non-running daemon with loaded models can also be healthy. | `const successful = value.errorKind === "";` | Enforce strict cross-field invariants and valid HTTP ranges. Require successful HTTP status and mutually consistent reachability, authorization, daemon, and error fields before reporting healthy. |
| 4 | block | dashboard-lmstudio/dashboard_lmstudio.ts:802-966,1549-1552 | Bundle and section IDs are fixed per method, so different endpoint/model inputs produce identical resource identities. Untrusted `errorKind` fragments are merely truncated, allowing separator injection, control characters, and truncation collisions in exception IDs. | `id: section.id` and `"lmstudio-health"` | Derive IDs from a canonical, non-reversible source discriminator plus method, with unambiguous encoding or hashing. Reject controls and delimiter characters; do not use raw truncated input as identity. |
| 5 | block | dashboard-lmstudio/dashboard_lmstudio.ts:466-478,720-744,1468-1479 | The expansion cap does not bound input parsing. The extension decodes, parses, and validates the entire resource before truncation, so a hostile response containing a huge array or string can still exhaust memory or CPU. | `return JSON.parse(new TextDecoder().decode(content));` | Enforce a byte limit before decoding and bounded field/list limits during parsing, or use a streaming bounded parser. Represent oversized input as an explicit partial/invalid observation. |
| 6 | operator-decision | dashboard-lmstudio/dashboard_lmstudio.ts:94-320,404-425 | The exported bundle contract deliberately uses `.passthrough()` at multiple levels, preserving unaudited attacker-controlled fields. This supports forward-compatible additive fields but can carry unexpected sensitive or oversized data through consumers. | `Unknown additive v1 fields survive parsing through passthrough schemas.` | Use strict schemas or an explicitly bounded extension namespace. Keeping passthrough means accepting persistence and propagation of fields outside the audited contract. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: see findings 1 and 2; accepted publisher, package, repository, and vendor identities were not reported.
2. Credential handling: see finding 1; no secret arguments, vault access, disk persistence, or URL credential construction was found.
3. Transport and network: no network calls or transport configuration exist in this report extension.
4. Untrusted input: see findings 1, 3, 4, 5, and 6; rendered Markdown does escape HTML and Markdown metacharacters.
5. Supply chain: only sanctioned `npm:zod@4` is imported; no remote dynamic import, undeclared dependency, evaluation, or fetched-code execution was found.
6. Documentation matches code: see findings 1 and 2; version, report name, artifact name, and single-request accounting otherwise match.
7. Data written: see finding 1 and the inventory below; prompts and completion content are not written.

## Data written by this extension
- Bundle provenance: source model type, configured model name, model ID, report/data names, version, timestamps, state, and accounting scope. Model names and IDs may identify private configuration.
- Endpoint health: reachability, authorization, HTTP status, latency, timestamp, classification, and error detail.
- Model inventory: exact count and up to 200 model identifiers.
- Daemon inventory: daemon state, loaded-model count, up to 200 loaded-model identifiers, timestamp, and error detail.
- Embedding probe: model identifier, capability, dimension, latency, HTTP status-derived findings, timestamp, and error detail.
- Completion probe: model identifier, finish reason, token counts, latency, context/output-limit flags, reasoning-only status, timestamp, and error detail; no prompt or generated content.
- Capability probe: model identifier, capability booleans, completed-check count, truncation state, latency, timestamp, and error classification.
- Markdown output: bundle state plus section title and summary; summaries can currently contain insufficiently sanitized collector errors.
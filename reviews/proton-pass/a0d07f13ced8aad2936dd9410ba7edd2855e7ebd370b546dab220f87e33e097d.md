# Secrets & Security Review
extension: @jpisgeek/proton-pass            version: 2026.08.30.1
content-hash: a0d07f13ced8aad2936dd9410ba7edd2855e7ebd370b546dab220f87e33e097d
reviewer: GPT-5 Codex   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | proton-pass/proton_pass.ts:387-401,409-415,452-473 | Item responses are not strictly validated. Nearly any object passes, and an arbitrary top-level string or first string inside an unknown `content` wrapper can be returned as the secret. This contradicts the claim that unrecognised shapes are refused. | `}).catchall(z.unknown())`, `asString(body[field])`, `for (const v of Object.values(content))` | Define strict response variants and explicit secret-bearing fields/wrappers. Reject unknown, ambiguous, or structurally incomplete responses. |
| 2 | block | proton-pass/proton_pass.ts:208-216 | The configured overall timeout does not cover binary resolution. Resolution receives only the caller signal, while up to four separate five-second probes can run after the configured deadline. | `bin = await resolveBinary(configured, opts.signal)` | Pass the combined caller/deadline signal into resolution while retaining enough state to classify cancellation versus timeout correctly. |
| 3 | block | proton-pass/proton_pass.ts:30-58,80-85,619-758 | Config values and secret locators accept control characters, while `clip()` preserves them before interpolation into logged exceptions. Newlines, NULs, and terminal escapes can corrupt or forge run-log entries. | `return s.length <= max ? s : ...`, `Secret '${clip(secretKey)}'` | Reject control characters in configuration and locators, or escape them before every diagnostic interpolation. |
| 4 | block | proton-pass/proton_pass.ts:170-175,262-267 | Execution errors can publish the operator’s configured or resolved local executable path into swamp logs and reports. | `Could not execute '${clip(bin)}'` | Use a fixed diagnostic or disclose only a non-identifying basename; never emit an absolute configured path. |
| 5 | operator-decision | proton-pass/README.md:79-99; proton-pass/proton_pass.ts:60-85,691-758 | Secret keys, vault names, item titles, field names, and complete `pass://` identifiers are deliberately written to exceptions that reach logs and reports. This exposes credential-inventory metadata and stable secret-store identifiers. | `Secret 'x/y' not found in vault 'z'` | Use opaque correlation IDs or redact/hash identifiers. Keeping this behavior accepts disclosure of credential-inventory metadata to every log/report reader. |
| 6 | operator-decision | proton-pass/README.md:61-77; proton-pass/proton_pass.ts:784-855 | `put()` deliberately places the secret value in process arguments, exposing it to local process inspection for the command’s lifetime. | `"--password", secretValue` | Pass the value through a protected stdin/file-descriptor mechanism if supported; otherwise remove or disable `put()`. Keeping it accepts local process-list disclosure. |
| 7 | operator-decision | proton-pass/README.md:100-109; proton-pass/proton_pass.ts:89-108 | Any executable selected through `PATH` or `binary` is trusted with vault names, item identifiers, and written secrets without ownership, signature, or digest verification. | `whichever executable answers --version first` | Require a trusted absolute path and verify ownership/signature or digest. Keeping discovery preserves convenience while accepting credential theft by a substituted executable. |
| 8 | operator-decision | proton-pass/README.md:100-109; proton-pass/proton_pass.ts:568-577 | A missing item `state` is deliberately treated as active. Older or malformed CLI responses can therefore make a trashed secret resolvable. | `const state = rec.state ?? "Active"` | Require `state: Active` and reject rows without state, or require a CLI version that supplies it. Keeping compatibility accepts weaker deletion enforcement. |
| 9 | operator-decision | proton-pass/README.md:100-109; proton-pass/proton_pass.ts:691-708 | A `pass://` item absent from the configured vault listing is passed through without a liveness check. A trashed item in another vault may still be returned. | `An id absent from this listing is left alone` | Reject IDs absent from the checked listing or obtain authoritative state for the URI’s actual vault. Keeping cross-vault pass-through accepts an incomplete deletion guarantee. |
| 10 | fix | proton-pass/README.md:89-96; proton-pass/proton_pass.ts:329-337,360-361 | Documentation says only a fixed verdict from the listed closed set escapes, but the implementation emits a dynamic exit-code suffix. | `unclassified (exit ${code})` | Document the actual bounded format or return exactly `unclassified`. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: see findings 3–5; no embedded credentials, private hostnames, personal domains, IP addresses, account IDs, real item names, or operator-specific paths were found. The LICENSE identity, registry handle, repository URL, and product names are accepted decisions.
2. Credential handling: see findings 4–7; no secret value is deliberately copied to swamp storage, resource attributes, filenames, URLs, or raw CLI error output.
3. Transport and network: see findings 2 and 7; the extension exposes no user-configurable network URL, cleartext mode, TLS-verification bypass, redirect handling, or non-HTTP fetch path.
4. Untrusted input: see findings 1 and 3; no shell, SSH, SQL, HTML rendering, or resource-identity collision surface was found.
5. Supply chain: checked and clean. The only import is the sanctioned runtime-provided `npm:zod@4`; no remote dynamic import, undeclared dependency, fetched executable code, `eval`, or `new Function` was found.
6. Documentation matches code: see findings 5–10; method names, argument names, key splitting, resource behavior, and example configuration otherwise match the implementation.
7. Data written: see finding 6; no swamp datastore resources are written, but `put()` writes an externally stored Proton Pass login item containing a title and secret password.

## Data written by this extension

- Swamp resources: none.
- Proton Pass: `put()` creates a login item whose title is `secretKey` and whose password is the sensitive `secretValue`.
- Runtime-only outputs: `get()` returns secret values and `list()` returns potentially sensitive item-title inventory; this implementation does not persist either to swamp resources.
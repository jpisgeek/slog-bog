# Secrets & Security Review
extension: proton-pass            version: 2026.08.30.1
content-hash: d05b9f684da53de489e55e4b9646cc2c4d78f1751a442e8fed589b2f866289a5
reviewer: GPT-5   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | README.md:50 | The example publishes a plausible real secret-store vault name rather than an obviously fake placeholder. | `vaultName: homelab` | Replace it with `<your-vault>` or an unmistakably fictional value such as `example-vault`. |
| 2 | operator-decision | proton_pass.ts:414-475; README.md:81-90 | Lookup failures write secret keys, item titles, vault names, and complete `pass://` identifiers into exceptions that reach logs and reports. This discloses secret-store inventory and account/vault identifiers. The README explicitly documents the diagnostic trade-off. | `Secret '${secretKey}' ... vault '${cfg.vaultName}'` | To remove the exposure, emit an opaque lookup reference or redacted/hash identifier instead. Keeping this behavior accepts disclosure of secret-store metadata in logs and reports. |
| 3 | operator-decision | proton_pass.ts:479-500; README.md:71-74 | `put()` exposes the secret value in the local process argument list. This is explicitly documented as a known consequence of retaining `put()`. | `"--password", secretValue` | Pass the secret through stdin or another non-argv interface if supported; otherwise remove or disable `put()`. Keeping it accepts temporary disclosure to local process observers. |
| 4 | operator-decision | proton_pass.ts:38-46,60-92 | A configurable executable and PATH lookup determine which program receives vault names, item identifiers, and `put()` secrets. No identity, ownership, signature, or allowlist check proves it is the official CLI. Custom executable resolution is documented as a feature. | `pass-cli executable... resolved against PATH` | Restrict execution to verified, trusted `pass-cli` locations or verify the selected binary. Keeping arbitrary paths/PATH resolution accepts executable-hijacking and credential-exfiltration risk. |
| 5 | block | proton_pass.ts:58-62 | Binary resolution is cached globally without being keyed by configuration. After one bare executable resolves, later provider instances using a different bare name silently execute the first program and may send secrets to it. | `if (resolvedBinary) return resolvedBinary;` | Cache resolutions by configured executable, or remove the global cache. |
| 6 | block | proton_pass.ts:60-93,102-109 | The `--version` probes run before the timeout is created and have no timeout or cancellation signal. A selected executable can hang indefinitely despite the documented per-call bound. | `await new Deno.Command(bin, { args: ["--version"] ... }).output()` | Apply the caller signal and configured timeout to every probe, including binary resolution. |
| 7 | block | proton_pass.ts:97-112,387-388,462-500 | `run()` can accept a caller signal, but `get()`, `put()`, and `list()` neither accept nor forward one. Caller cancellation therefore is not honoured. | `get: async (secretKey: string)` and `{ timeoutSec: cfg.timeoutSec }` | Accept the platform cancellation context/signal and pass it through every list, view, create, and probe invocation. |
| 8 | block | proton_pass.ts:365-382,398-423; README.md:94-95 | The promised liveness/trash check for `pass://` URIs is dead code. The URI branch never extracts or returns `id`, so `if (id !== undefined)` cannot run and a trashed URI may still resolve. | `return { args: [secretKey], field, title: undefined };` | Parse and validate the URI identifiers, then perform the intended live-item check before viewing the item. |
| 9 | operator-decision | proton_pass.ts:420-422; README.md:111-116 | A `pass://` URI absent from the configured vault is deliberately passed through because it may access another vault. This means `vaultName` is not an access boundary and the provider can retrieve secrets outside it. | `a pass:// URI can address a different vault` | Reject URIs not proven to belong to the configured vault. Keeping this behavior accepts cross-vault access through one provider configuration. |
| 10 | block | proton_pass.ts:216-279 | Item-view JSON is not validated against strict response schemas. Recursive blind casts search every nested object and may return an unrelated same-named string from an unexpected response shape. | `const rec = entry as Record<string, unknown>` | Define strict versioned schemas for supported CLI response shapes, reject unknown structures, and extract only from validated field locations. |
| 11 | block | proton_pass.ts:301-345 | Item-list parsing disguises malformed responses as an empty or incomplete vault. Non-JSON text with no matching rows returns `[]`; malformed JSON rows are silently skipped; missing IDs are backfilled with `""`. | `if (!m) continue`, `if (typeof title !== "string") continue`, `id: ... ? rec.id : ""` | Reject unmatched nonempty text and reject rows missing required fields. Use strict schemas for every supported output variant. |
| 12 | operator-decision | proton_pass.ts:335-338; README.md:111-114 | A missing `state` is deliberately converted to `Active`. On CLI versions that omit state for trashed items, deleted secrets can remain listable and resolvable. | `const state = typeof rec.state === "string" ? rec.state : "Active";` | Require `state` or a CLI version that supplies it. Keeping compatibility with older output accepts failure to enforce deletion state. |
| 13 | block | README.md:79; proton_pass.ts:164-178 | The README says CLI stderr or `Error:` lines are carried in error messages, but the code classifies them and emits only a fixed verdict. The published security documentation is contradictory and does not match the code. | `Error messages carry pass-cli's stderr or its Error: lines only.` | State that both sources are inspected but never forwarded, and list the fixed classifications actually emitted. |
| 14 | block | proton_pass.ts:9-12,373-377 | The module-header key examples do not match the first-slash parser. `Example Service/API Key` actually selects item `Example Service`, field `API Key`; the three-segment example produces field `API Key/password`. | `"Example Service/API Key" -> item titled so, field = defaultField` | Replace the examples with forms that exactly reflect first-slash parsing. |
| 15 | block | proton_pass.ts:197-206; README.md:87-89 | Failure classification checks generic `not found` before `vault ... not found`, so common vault-not-found messages are mislabeled `item-not-found` despite the documented fixed verdict set. | `[/not found.../, "item-not-found"]` precedes `[/vault .*not found.../, "vault-not-found"]` | Match specific vault and field failures before the generic item-not-found pattern. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information — see finding 1; no static credentials, tokens, private keys, personal paths, addresses, control characters, or unaccepted identities were found.
2. Credential handling — see findings 2–5; retrieved secret values are not cached, persisted, or placed directly in ordinary error messages.
3. Transport and network — see findings 4, 6, and 7; no URL configuration, cleartext opt-out, TLS-verification bypass, or redirect handling exists because network transport is delegated to the CLI.
4. Untrusted input — see findings 8–12; shell concatenation, SQL, HTML rendering, and resource-ID collisions were not present.
5. Supply chain — clean: `npm:zod@4` is the sanctioned runtime import; there are no other imports, dynamic remote imports, fetched executable code, `eval`, or `new Function`.
6. Documentation matches code — see findings 8, 13, 14, and 15.
7. Data written — no swamp resources or datastore records are written; external and diagnostic outputs are listed below.

## Data written by this extension
- Swamp resources/datastore: none.
- Proton Pass: `put()` creates a login item whose title is derived from `secretKey` and whose password is `secretValue`; both are sensitive, and the password is exposed through process argv.
- Run logs/reports on failure: may contain the secret key/item path, vault name, item title, and `pass://` identifiers.
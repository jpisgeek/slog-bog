# Secrets & Security Review
extension: proton-pass            version: 2026.08.30.1
content-hash: 0f96ac5cfba0226bf805c10e875e785f0b46cc35803f03269e9a4d5da2ebbcce
reviewer: Codex (GPT-5)   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | README.md:28,48; proton_pass.ts:22 | A plausible real secret-store vault name is published instead of an unmistakably fake placeholder. | `'homelab'` | Replace it everywhere with `<your-vault>` or `YOUR_VAULT`. |
| 2 | block | proton_pass.ts:116-139 | Raw CLI stderr and selected stdout are copied into exceptions that may enter logs and reports. The CLI may echo sensitive arguments or item metadata. | `const detail = stderr \|\| stdoutErrors` | Map known failures to constant messages. Never relay subprocess output without strict redaction. |
| 3 | block | proton_pass.ts:203-205,332-371 | Exceptions explicitly disclose vault names, item titles, fields, and complete secret lookup keys. | ``Secret '${secretKey}' ... vault '${cfg.vaultName}'`` | Return opaque error codes and generic messages; keep secret-store locators out of exceptions. |
| 4 | operator-decision | README.md:69-72; proton_pass.ts:376-397 | `put()` deliberately places the secret value in process arguments, exposing it to other local users and process inspection. | `"--password", secretValue` | Use stdin or another protected channel if supported, or remove/disable `put()`. Keeping it accepts transient local credential disclosure. |
| 5 | block | proton_pass.ts:52-94 | CLI probes and operations have no timeout or AbortSignal, so network-backed calls can hang indefinitely and caller cancellation is ignored. | `new Deno.Command(...).output()` | Apply bounded timeouts, honor the caller’s AbortSignal, terminate the child, and distinguish cancellation from timeout or remote failure. |
| 6 | block | proton_pass.ts:150-208,229-264 | CLI responses are not validated with strict schemas. The code uses blind casts and recursive heuristic field discovery; any nonempty malformed non-JSON output can be returned as a secret. | `if (trimmed) return trimmed` | Define strict schemas for every supported output shape and reject malformed, unknown, or ambiguous responses. Permit a bare-value response only when positively identified. |
| 7 | block | proton_pass.ts:234-264,322-354 | Malformed item-list responses and invalid rows are silently converted into an empty or incomplete inventory. Missing IDs become `""`, after which lookup falls back to ambiguous title resolution. | `if (!Array.isArray(rows)) return []` | Reject malformed responses and require nonempty IDs, titles, and valid state values. Never turn parsing failure into “no items” or fall back from a selected item to its title. |
| 8 | operator-decision | proton_pass.ts:257-264 | Missing item state is deliberately treated as active for compatibility. On a CLI response without state, trashed entries can therefore be considered live. | `const state = ... ? rec.state : "Active"` | Require state and a supported CLI version to guarantee deletion semantics, or explicitly accept and document that older clients cannot guarantee trashed-item exclusion. |
| 9 | block | proton_pass.ts:289-295,319-320 | `pass://` lookups bypass the live-item/state check, so the provider itself does not enforce the README’s “deleted means deleted” guarantee for stable URIs. | `Skipped for pass:// URIs` | Resolve or inspect the referenced item and verify its state is active before reading its value. |
| 10 | block | proton_pass.ts:9-12,296-305; README.md:55-57 | `/` is an unescaped separator that may occur in titles or fields, creating ambiguous identities. The source documentation also says `Example Service/API Key` names that whole title, while the implementation parses `Example Service` as the title and `API Key` as the field. | `const slash = secretKey.indexOf("/")` | Use an unambiguous structured or escaped locator, or reject slash-bearing titles and require `pass://` URIs for them. Correct all published examples. |
| 11 | block | README.md:76-93 | The security documentation overstates guarantees contradicted by the implementation: errors contain private locators, malformed output can become a value, missing state is accepted as active, and URI lookups skip deletion checks. | `no value is ... logged`; `Deleted means deleted.` | Correct the implementation, then rewrite the security claims to describe its actual guarantees and residual limitations exactly. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
Section 1 — See finding 1; no credentials, tokens, private keys, emails, private hosts, account IDs, IP addresses, control characters, or operator-specific filesystem paths were otherwise found.
Section 2 — See findings 2–4; no direct resource or disk persistence by the extension was found.
Section 3 — See finding 5; there are no configurable network endpoints, cleartext modes, TLS-verification controls, redirects, or URL-userinfo handling in the extension itself.
Section 4 — See findings 6–10; no HTML, shell-string construction, SQL, or API-derived filesystem paths were found.
Section 5 — The sole import is the sanctioned `npm:zod@4`; no undeclared imports, dynamic remote imports, fetched executable code, `eval`, or `new Function` were found.
Section 6 — See findings 9–11; manifest type, version, method names, and declared published files otherwise match the supplied code and README.
Section 7 — See findings 3 and 4; no swamp resources are written, but `put()` writes sensitive data to Proton Pass.

## Data written by this extension
- Swamp resource kinds: none.
- `put()` writes a Proton Pass login item containing an item title (sensitive secret-store metadata) and password (secret).
- `get()` returns secret values to its caller; `list()` returns live item titles.
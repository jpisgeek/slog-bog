# Secrets & Security Review
extension: proton-pass            version: 2026.08.30.1
content-hash: aba872f353521d6a84beb81afe500991c19829fc59faaa8eb272bf45a6d3edf2
reviewer: GPT-5 Codex   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | README.md:79-91 | The README contradicts the implementation and itself about forwarding CLI error text. | `Error messages carry pass-cli's stderr or its Error: lines only.` | State consistently that CLI output is classified into fixed verdicts and never forwarded verbatim. |
| 2 | block | README.md:48-51 | The optional `binary` example is outside `config`; uncommenting it produces the wrong configuration shape. | `# binary: /opt/homebrew/bin/pass-cli` | Indent `binary` beneath `config`. |
| 3 | block | proton_pass.ts:258-330 | Secret-bearing API responses are not validated against strict schemas. A recursive key search can silently return an unrelated string as the secret. | `for (const value of Object.values(rec))` | Define strict versioned response schemas and extract only documented field locations; reject every unknown shape. |
| 4 | block | proton_pass.ts:353-415 | Item-list parsing accepts partial human-readable output and treats blank output as an empty vault, disguising malformed responses as valid state. | `if (!m) continue;` | Require every nonblank row to parse, reject blank output unless explicitly valid, and validate JSON with strict schemas. |
| 5 | block | proton_pass.ts:395-410,545-553 | Missing item IDs are replaced with `""`; lookup then falls back to title selection, which the code itself says can select a duplicate or trashed item. | `id: typeof rec.id === "string" ? rec.id : ""` | Require a valid nonempty ID before claiming exact resolution; otherwise fail closed. |
| 6 | block | proton_pass.ts:76-158 | Caller cancellation is not honored during binary resolution, and simultaneous timeout/cancellation is reported as timeout because the deadline is checked first. | `const bin = await resolveBinary(configured);` | Pass the caller signal through resolution and probes, and classify caller cancellation before timeout. |
| 7 | block | proton_pass.ts:587-600 | `put()` does not round-trip documented qualified keys: `Item/field` stores a password under `Item`, while `get()` later requests `field`; a `pass://` key becomes title `pass:`. | `const title = slash === -1 ? secretKey : secretKey.slice(0, slash);` | Implement matching key semantics or reject and document unsupported qualified and URI keys. |
| 8 | operator-decision | proton_pass.ts:504-573 | Secret-store item names, vault names, and fields are placed in thrown errors that reach logs and reports. | ``Secret '${secretKey}' ... vault '${cfg.vaultName}'`` | Accept disclosure of secret identifiers for diagnostics, or redact them and use opaque correlation identifiers. |
| 9 | operator-decision | proton_pass.ts:577-600 | `put()` exposes the secret value in process arguments to other local users. | `"--password", secretValue` | Accept this local disclosure, use a protected stdin/body mechanism, or remove `put()`. |
| 10 | operator-decision | proton_pass.ts:404-412 | An absent `state` is deliberately interpreted as active, so older or malformed responses can make a trashed secret resolvable. | `const state = typeof rec.state === "string" ? rec.state : "Active";` | Accept the compatibility risk, or require `state` and fail closed. |
| 11 | operator-decision | proton_pass.ts:492-514 | A `pass://` item absent from the configured vault is used without a liveness check; a trashed item in another vault may therefore remain resolvable. | `An id absent from this listing is left alone` | Accept cross-vault URIs without deletion enforcement, or restrict URIs to a vault whose state can be verified. |
| 12 | operator-decision | proton_pass.ts:46-53,76-118 | The provider executes the first matching PATH binary or any configured path without verifying that it is the official CLI. A shadow binary can receive secret metadata and `put()` values. | `if (await probe(configured))` | Accept PATH/custom-binary trust, or require an authenticated executable with ownership, signature, or digest verification. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: no real credentials, private infrastructure identifiers, operator paths, hidden control characters, or unapproved identities found in the published files.
2. Credential handling: see findings 8 and 9; no secret persistence to swamp storage or disk was found.
3. Transport and network: see finding 6; no configurable URL, cleartext transport option, TLS-disable option, or unsupported scheme was found.
4. Untrusted input: see findings 3–5, 7, 10, and 11; no shell interpolation, SQL, file-path construction, or HTML rendering was found.
5. Supply chain: see finding 12; `npm:zod@4` is the sanctioned runtime import, with no remote dynamic imports, evaluation, or undeclared package imports found.
6. Documentation matches code: see findings 1, 2, 7, and 11.
7. Data written: no swamp resources are written; README discloses that `put()` creates remote login items, subject to finding 9.

## Data written by this extension
- No swamp datastore resources.
- `put()` creates a Proton Pass login item whose title derives from the secret key and whose password contains the sensitive secret value.
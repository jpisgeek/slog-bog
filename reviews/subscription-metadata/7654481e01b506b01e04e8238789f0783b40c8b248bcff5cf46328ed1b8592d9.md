# Secrets & Security Review
extension: @jpisgeek/subscription-metadata            version: 2026.09.05.1
content-hash: 7654481e01b506b01e04e8238789f0783b40c8b248bcff5cf46328ed1b8592d9
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all five published files, including comments, literals, examples, and prose; found no credentials, private infrastructure identity, local paths, control characters, or non-vendor domains. The publisher name, public package scope, and named vendors are accepted public identities.
2. Credential handling: this extension accepts no credentials. Both source-reference arguments are marked sensitive on the capture schema, reject userinfo, queries, and fragments, and permit only fixed public vendor origins and paths; resource tags contain only a constant provenance marker. Report Markdown contains no configured values, while JSON fields and references carry sensitive classification.
3. Transport and network: there are no network calls or transport options. Stored references require HTTPS and exact allowlisted public vendor origins and paths.
4. Untrusted input: capture and report independently enforce strict matching snapshot schemas, finite numbers, paired price/currency, ordered renewal dates, unique stable limit identities, and forbidden-derived-value guards. Invalid or absent data becomes informational unknown rather than healthy. No input reaches HTML, shell, SQL, or filesystem paths; the bundle identifier is a domain-separated SHA-256 digest of the model identifier.
5. Supply chain: the only dependency is the sanctioned runtime import `npm:zod@4`; there are no dynamic imports, fetched code, evaluation, or deserialization into code.
6. Documentation matches code: verified the model type and version, single `capture` method, `snapshot` resource and 365-day lifetime, argument names and constraints, report behavior, sensitivity statements, no-network claim, and example configuration against the implementation.
7. Data written: see below. The snapshot can contain commercially sensitive subscription metadata but no credential or personal inventory; the README explicitly warns users to protect both snapshot and report JSON.

## Data written by this extension
- `snapshot` resource, retained for 365 days: provider; optional plan name, billing cadence, exact decimal price in currency minor units, currency, renewal window, seat count, declared limits, fixed-public-vendor source references, and operator-config capture provenance. These commercial fields are sensitive; resource tags contain only `provenance=operator-config`.
- Report JSON: a sensitive dashboard bundle containing normalized versions of the configured subscription facts, a hashed model-instance bundle id, and the operator-chosen model name classified as private identity; the raw model id is withheld. Report Markdown contains only a fixed sensitivity notice and no configured values.

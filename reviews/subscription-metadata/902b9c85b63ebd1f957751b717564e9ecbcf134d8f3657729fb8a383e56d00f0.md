# Secrets & Security Review
extension: subscription-metadata            version: 2026.08.25.2
content-hash: 902b9c85b63ebd1f957751b717564e9ecbcf134d8f3657729fb8a383e56d00f0
reviewer: GPT-5   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | dashboard_subscription.ts:605-613; README.md:84-86 | Accepted prices can lose precision during conversion, contradicting the documented lossless guarantee. For example, `999999999999999.9999` becomes `1000000000000000`. | `value: Number(snapshot.priceMinor)` | Preserve the decimal as a string, or constrain and verify accepted values with an exact lossless round-trip test. |
| 2 | block | subscription_metadata.ts:56-70,145-149; dashboard_subscription.ts:466-472,506-510 | The model and report snapshot schemas do not accept and reject the same data. Nested limits and provenance strip unknown fields on write-schema parsing but are strict on report reads, despite the published parity claim. | `}).superRefine((limit, ctx) => {` versus `}).strict().superRefine((limit, ctx) => {` | Make both copies identically strict, including `LimitSchema` and `provenance`, and verify parity. |
| 3 | block | dashboard_subscription.ts:627-630 | Declared-limit metric IDs are positional. Different limits in the same array position receive the same ID, and reordering changes identity. | `id: \`declared-limit-${index + 1}\`` | Derive a stable collision-resistant ID from canonical limit identity and reject duplicate identities. |
| 4 | block | dashboard_subscription.ts:550-555,605-682,727-739 | Commercially sensitive billing fields and private model identity are emitted as `operational`, unredacted data. This conflicts with the code’s own sensitivity note and can cause publication-aware consumers to expose them. | `classification: "operational"` and `note: "Subscription and billing metadata can be commercially sensitive"` | Classify affected bundle, section, facts, metrics, references, model name, and model ID as sensitive; enumerate every sensitive field and apply redaction where appropriate. |
| 5 | operator-decision | subscription_metadata.ts:13-20; dashboard_subscription.ts:709-714; README.md:78-79,94-97 | Generic source URLs are a documented feature, but arbitrary HTTPS hostnames and paths can contain private infrastructure, account identifiers, or path-carried secrets. They are persisted for 365 days and emitted unchanged. | `url: snapshot.sourceReference` | Explicitly accept this disclosure risk, or restrict references to approved public vendor origins and safe paths; reject private/local hosts, IP literals, opaque account paths, and secret-like path segments. |
| 6 | fix | subscription_metadata.ts:170-175; README.md:50-54 | The README does not disclose that the provider is duplicated into datastore tag metadata, which may have different visibility from resource content. | `{ tags: { provider: snapshot.provider, provenance: "operator-config" } }` | Document both tags and their exposure, or remove the provider tag. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. See findings 4–5; no embedded credentials, operator hostnames, IP addresses, local paths, email addresses, secret-store names, NUL bytes, or hidden control bytes were found.
2. See findings 4–5; no intended credential argument, credential logging, URL userinfo, or disk secret persistence was found.
3. No network calls exist; HTTPS is required for stored references, and TLS, redirect, timeout, and cancellation concerns are otherwise inapplicable.
4. See findings 2–3; no shell, SSH, SQL, filesystem interpolation, API-response parsing, HTML rendering, or false healthy fallback was found. Markdown output is escaped.
5. Only the sanctioned `npm:zod@4` import is used; no undeclared, dynamic, executable, or unpinned dependency was found.
6. See findings 1–3 and 6; manifest names, version, model, report, method, arguments, resources, and example configuration otherwise match.
7. See findings 4–6; the README otherwise identifies the stored commercial sensitivity and the snapshot’s declared fields.

## Data written by this extension
- `snapshot` resource, lifetime 365 days: provider, plan name, billing cadence, decimal price, currency, renewal timestamps, seats, declared limits, source references, and capture provenance.
- Resource tags: provider and fixed `operator-config` provenance.
- Report output: Markdown summary plus dashboard JSON containing subscription facts, metrics, source URL, model name, model ID, and capture timestamp.
- Commercially sensitive fields include plan, price, renewal window, seats, limits, and potentially provider. Source URLs and model identity can expose private infrastructure, account identifiers, or secrets.
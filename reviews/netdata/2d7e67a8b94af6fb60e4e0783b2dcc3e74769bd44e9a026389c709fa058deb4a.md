# Secrets & Security Review
extension: @jpisgeek/netdata            version: 2026.08.23.1
content-hash: 2d7e67a8b94af6fb60e4e0783b2dcc3e74769bd44e9a026389c709fa058deb4a
reviewer: claude-fable-5      date: 2026-08-23
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | operator-decision | netdata/netdata.ts:37, netdata/README.md:65,91 | Cleartext HTTP to the agent is accepted and is what the README example uses. No credential crosses the wire, but an on-path party can read hostname/OS/version/alarm text/mount paths and rewrite responses (clear a firing alarm, inject text into stored `error`/`info`). README documents this honestly. | `u.protocol === "http:" \|\| u.protocol === "https:"`; example `url: http://nas.example.com:19999` | Operator rules. If tightening: require `https:` unless `ssh` is set, or gate `http:` behind an explicit per-node `allowInsecureHttp: true`. |
| 2 | fix | netdata/netdata.ts:714–754, 765–777 | A reachable node whose `/alarms` or `/charts` sub-fetch failed is written with carried-forward counts and `error: ""`. On the first poll (no `prevNode`) that yields `reachable: true, alarmsActive: 0, alarmsCritical: 0` — a failed fetch reads as healthy. On later polls stale counts are indistinguishable from fresh ones; only `summary.nodesDegraded` (fleet-wide, not per node) and the log say otherwise. | `alarmsActiveVal = r.alarmsOk ? r.alarms.length : Number(prevNode?.alarmsActive ?? 0)`; node record has no freshness/degraded field | Add per-node `alarmsFresh`/`chartsFresh` booleans (or a `degraded` flag + `lastFullPollAt`) to `NodeStateSchema`, write them, document them in README "Data written". |
| 3 | fix | netdata/netdata.ts:426,435,443,461,476,659,724 | API responses are not schema-validated; blind `as Record<string, unknown>` / `as { labels?: ...}` casts. Missing alarm `value` → `0`, missing `cores_total` → `0`, missing `status` → `""` (counted active but neither critical nor warning). A non-numeric `value` yields `NaN` which `z.number()` rejects at write time, failing the whole sweep mid-write. | `value: Number(a.value ?? 0)`; `cores: Number(info.cores_total ?? 0)` | Define zod schemas for `/info`, `/alarms`, `/charts`, `/data` with `.safeParse`; treat unparseable payloads as a failed sub-fetch; make `value`/`cores` nullable rather than defaulting to 0. |
| 4 | fix | netdata/netdata.ts:705, 643, 672 | Node instance name is `node-${slug(name)}` with no hash: distinct names that slug identically (`NAS`/`nas`, `db 1`/`db-1`) pass the raw-name uniqueness check and overwrite each other's node record. Separately, `protectedPrefixes` (`alarm-<slug>-`) is built independently of `instanceName()`, whose label is truncated at 48 chars — a node slug ≥47 chars makes the prefix never match and the prune deletes that node's alarm/mount records; and `alarm-nas-` also matches node `nas-2` (over-protection, benign). | `const nn = \`node-${slug(r.name)}\``; `protectedPrefixes.push(\`alarm-${slug(r.name)}-\`)`; `.slice(0, 48)` | Enforce uniqueness on `slug(name)` (or append hash to node names); derive protection from the stored `node` tag instead of a name prefix, or cap/validate node name length. |
| 5 | fix | netdata/netdata.ts:449, 503, 516 | Caller cancellation (`ctx.signal` abort) during a sub-fetch is caught by the inner `catch`, logged as a remote failure, and `pollNode` returns a "degraded" node. If no later poll reaches the outer catch (abort during the last node's sub-fetch), `discover()` proceeds to write records and run the prune. The outer-catch rethrow only covers `/info`. | only `if (signal.aborted) throw e;` at :532 | Add `if (signal.aborted) throw e;` at the top of each inner catch (alarms, charts, per-mount). |
| 6 | fix | netdata/netdata.ts:408 | Direct `fetch` follows redirects (default `redirect: "follow"`), including an `https:`→`http:` downgrade; the ssh path (`curl -s`, no `-L`) does not follow, so a 3xx there becomes a JSON-parse failure. Exposure is low (the configured server/proxy itself must issue the redirect; an on-path actor cannot inject one into TLS). | `await fetch(\`${base}${path}\`, { headers: ..., signal: ... })` — no `redirect` option | Set `redirect: "error"` (or `"manual"` + require same scheme) for parity with the ssh path. |
| 7 | note | netdata/netdata.ts:404, 420 → 246 | V8 `JSON.parse`/`res.json()` SyntaxErrors embed a ~10-char snippet of the body (`Unexpected token '<', "<!DOCTYPE "... is not valid JSON`); for the `/info` call this reaches the fallback branch of `sanitizeNodeError` and is persisted (≤120 chars). Body is from the operator's own agent/proxy; no credential path. | `return raw.replace(/\S+@\S+/g, "<host>").slice(0, 120);` | Map `SyntaxError` → `"invalid JSON response"` before the fallback. |
| 8 | note | netdata/netdata.ts:358–359; netdata/README.md:98–99 | "unknown host keys fail closed" holds only under ssh's default `StrictHostKeyChecking=ask`; a user/global ssh_config with `no`/`accept-new` silently relaxes it because the code does not pin it. | args: `"-o", "BatchMode=yes"` only | Pass `-o StrictHostKeyChecking=yes` explicitly, or soften the README claim to "with default ssh config". |
| 9 | note | netdata/README.md:97 vs netdata/netdata.ts:38; README.md:28 | README says `url` is restricted to http(s)/no userinfo but omits the single-quote rejection; ssh transport prerequisites (local `ssh` on PATH, `curl` on the node) are not stated. `SECURITY.md` link target is outside the published set — not verified here. | `!v.includes("'")` | Regenerate README from source descriptions; add prerequisites line. |
| 10 | note | netdata/README.md:69 | `127.0.0.1` literal in the example. Canonical loopback, the required value for the documented ssh pattern, not identifying. | `url: http://127.0.0.1:19999` | None. |
| 11 | note | netdata/LICENSE:3 | Real name in copyright line publicly ties the `jpisgeek` handle to a legal name. Standard MIT practice and evidently deliberate. | `Copyright (c) 2026 Jason Powell` | Confirm intent; no code change. |
| 12 | note | netdata/netdata.ts:196 | Comment references the sibling `truenas` extension's former internals (`RAW NUL`). Not identifying; cosmetic cross-reference to a public sibling. | `truenas previously used a RAW NUL here` | Optional: drop the cross-reference. |
| 13 | note | netdata/netdata.ts:455, 511, 522, 542 | Log lines (not resources) carry raw ssh stderr (can echo local key paths like `Load key "/Users/…"`), `user@host`, and ≤200 chars of HTTP body. README discloses this. Relevant only if logs are shipped off-box. | `error: rawMsg.slice(0, 300)` | None unless logs leave the host. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets/identifying info: scanned all four files (code, comments, string literals, README prose, example YAML, license). No credentials, tokens, keys, PEM blocks, real hostnames/domains (only `*.example.com`), RFC1918/public IPs, MACs, serials, emails, org IDs, secret-store paths, local filesystem paths, or raw control bytes (`\u001f` is an escape, :198). Residual: notes 10–12.
2. Credential handling: no argument carries a secret, so no `sensitive` marking is required; `url` userinfo rejected (:38); persisted `error` is class-only via `sanitizeNodeError` (:223–247); no secret in tags, instance names, URLs, or disk. Residual: note 7 (body snippet), note 13 (logs).
3. Transport/network: TLS verification not disabled anywhere (no `-k`, no fetch TLS override); `file:`/`data:`/other schemes rejected (:37); timeouts on every call (fetch :410; ssh ConnectTimeout :361, curl `--max-time` :353, outer abort :372); caller signal honoured and cancellation rethrown for `/info` (:532). Findings 1, 5, 6, 8.
4. Untrusted input: no HTML rendering; no SQL; no local shell — `Deno.Command` argv array (:356–366); remote command is single-quoted with `'` stripped from the path and rejected from the URL (:38, :354), `host`/`user` guarded against leading `-` (:58, :61); alarm/mount instance names hash the raw `\u001f`-joined identity (:198–213). Findings 2, 3, 4.
5. Supply chain: sole import `npm:zod@4` (:21) — the sanctioned bare-major specifier. No other imports, no dynamic `import()`, no `eval`/`new Function`, nothing fetched and executed; manifest `dependencies: []`. Clean.
6. Docs vs code: argument names/types/defaults, method name and single `node` argument, all four resource field lists (incl. `nodesDegraded`), versions (manifest/model/README all `2026.08.23.1`), hash-suffix caveat, and Security section claims match the source. Residual: notes 8, 9.
7. Data written: enumerated below; README "Data written" and "Security" paragraphs disclose hostnames, OS, version, URL, alarm text, mount paths. No write can include a credential (only `url` is operator-supplied and userinfo is rejected).

## Data written by this extension
- `node` (infinite, gc 10) — `name`, `url` (operator-supplied, persisted verbatim; may be an internal hostname/IP or obscurity path), `reachable`, `error` (sanitized class), `transport`, `version`, `hostname` (node's real hostname, from `/alarms`), `osName`, `osVersion`, `cores`, `collectors`, `charts`, `alarmsActive`, `alarmsCritical`, `alarmsWarning`, `claimedToCloud`, `mountsOverThreshold`. Tags: `reachable`, `os`, `alarmsCritical`.
- `alarm` (infinite, gc 50) — `node`, `name`, `chart`, `status`, `value`, `units`, `info` (free text from Netdata's health engine). Tags: `node`, `status`.
- `mount` (infinite, gc 30) — `node`, `mount` (filesystem path as encoded in the chart id; on macOS can include `/Volumes/<label>`), `availGiB`, `usedGiB`, `totalGiB`, `usedPercent`, `overThreshold`. Tags: `node`, `overThreshold`.
- `summary` (infinite, gc 30) — `nodes`, `nodesReachable`, `nodesUnreachable`, `nodesDegraded`, `alarmsActive`, `alarmsCritical`, `mountsOverThreshold`, `syncedAt`. Tags: `alarmsCritical`, `nodesDegraded`.
- Personal/sensitive: hostnames, OS/version fingerprints, agent URLs, mount paths — infrastructure inventory of the operator's own fleet, stored in the operator's datastore; disclosed in README. No MACs, user lists, or credentials.

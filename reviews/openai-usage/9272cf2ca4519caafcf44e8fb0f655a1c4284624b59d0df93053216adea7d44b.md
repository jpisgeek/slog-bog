# Secrets & Security Review
extension: openai-usage            version: 2026.08.25.2
content-hash: 9272cf2ca4519caafcf44e8fb0f655a1c4284624b59d0df93053216adea7d44b
reviewer: GPT-5 Codex   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | README.md:48,55 | Example secret-store and model names are not unmistakably fake placeholders and could disclose the operator's naming conventions. | `vault.get("example-vault", "OPENAI_ADMIN_KEY")`; `openai-org collect` | Use explicit placeholders such as `YOUR_OPENAI_ADMIN_KEY_ITEM` and `<your-model-name>`. |
| 2 | block | openai_usage.ts:155-176 | API page envelopes are not strictly validated. Missing or wrongly typed `has_more` is treated as false, marking potentially incomplete data complete and healthy. | `const hasMore = object.has_more === true`; `if (!hasMore) ... state: "complete"` | Parse every page through strict schemas requiring correctly typed pagination, bucket, and result fields. Reject inconsistent or missing pagination metadata. |
| 3 | block | openai_usage.ts:269-270,287-288,352-353 | Malformed response values are normalized as absent: null cached-token values become zero, while non-string identifiers become null. This hides protocol violations. | `value === undefined \|\| value === null`; `typeof item.project_id === "string" ? item.project_id : null` | Distinguish contractual absence from present-but-invalid values. Reject invalid types and degrade the affected dimension. |
| 4 | block | openai_usage.ts:139-142; README.md:76 | Fetch follows redirects by default, with no validation of the redirected protocol or hostname. Requests therefore are not guaranteed to remain on the documented official HTTPS origin. | `fetcher(..., { headers: ..., signal })`; `Requests are fixed to the official HTTPS API origin.` | Disable redirects or manually follow only HTTPS redirects whose hostname is exactly `api.openai.com`; never forward authorization elsewhere. |
| 5 | block | dashboard_openai.ts:502-507,767-775; README.md:73-77 | Dashboard JSON includes the local Swamp model name and ID, but its sensitivity metadata omits them and the README does not clearly disclose them. | `modelName: ctx.definition.name`; `modelId: ctx.modelId`; `fields: ["projectId", "model", "lineItem"]` | Add producer model name/ID to sensitivity metadata and README disclosure, or omit them from report output. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information — see finding 1; no actual credentials, private hosts, addresses, local paths, or unaccepted identities were found.
2. Credential handling — the API key is marked sensitive, used only in the Authorization header, and excluded from URLs, errors, resources, reports, logs, and disk.
3. Transport and network — see finding 4; the initial origin is HTTPS, TLS verification is not disabled, calls have timeouts, and caller cancellation is honored.
4. Untrusted input — see findings 2, 3, and 5; no shell, SQL, SSH, filesystem, or unsafe HTML interpolation was found.
5. Supply chain — only sanctioned `npm:zod@4` is imported; no dynamic code loading, evaluation, or undeclared dependency was found.
6. Documentation matches code — see findings 1, 4, and 5; method, arguments, resource schema, lifetime, version, and failure-state documentation otherwise match.
7. Data written — see finding 5; snapshot breakdown disclosure is otherwise present and no credential is written.

## Data written by this extension
- `snapshot` resource, retained 90 days: coverage timestamps, availability statuses, aggregate token/request/cost totals, OpenAI project IDs, model names, and line-item names. Project IDs and workload labels are identifying operational data; the README warns about them.
- Dashboard JSON report: aggregate metrics and statuses plus local Swamp model name and ID. These identifiers are insufficiently classified and disclosed; see finding 5.
- Dashboard Markdown report: overall and per-section states with aggregate summaries; no credential or breakdown identifier is included.
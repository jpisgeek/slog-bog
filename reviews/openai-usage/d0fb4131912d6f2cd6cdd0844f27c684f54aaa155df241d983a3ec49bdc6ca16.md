# Secrets & Security Review
extension: @jpisgeek/openai-usage            version: 2026.09.05.1
content-hash: d0fb4131912d6f2cd6cdd0844f27c684f54aaa155df241d983a3ec49bdc6ca16
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed the complete five-file published surface and reconciled it to the new content hash. Found no credential value, private hostname, account identifier, local path, control character, or non-vendor domain. The publisher name, public package scope, repository, and OpenAI product/API names are accepted public identities.
2. Credential handling: the Admin API key is required and marked sensitive, used only in the Authorization header, and absent from URLs, response-derived messages, resources, tags, and reports. Errors use fixed local messages and never persist response bodies, server text, or redirect locations. The documented vault names are explicit placeholders.
3. Transport and network: requests use fixed official HTTPS OpenAI endpoints, do not expose a URL override, refuse redirects, apply per-request and whole-walk time limits, and combine timeout with caller cancellation while preserving caller aborts.
4. Untrusted input: API page, bucket, and endpoint-specific row schemas are strict; numeric totals are checked after aggregation; cursors cannot repeat; pagination has page and elapsed-time caps; malformed, incomplete, failed, or missing dimensions become partial/unavailable rather than healthy zeroes. The bundle ID is a provider-specific, purpose-separated full SHA-256 of stable `ctx.modelId`, so distinct instances coexist, renames remain stable, and raw identity is absent from the ID. Nothing reaches HTML, shell, SQL, or filesystem paths.
5. Supply chain: the only dependency is the sanctioned runtime import `npm:zod@4`; there are no dynamic imports, fetched code, evaluation, or deserialization into code. The inlined dashboard contract remains the reviewed canonical contract.
6. Documentation matches code: verified the model type and version, arguments, single `collect` method, `snapshot` resource and 90-day lifetime, two fixed API walks, strict validation behavior, independent partial states, pagination caps, stable hashed bundle identity, report fields, examples, and security statements against the implementation.
7. Data written: see below. Stored project IDs, model names, line items, usage, billed cost, and producer model identity can expose operational or internal workload information; the README explicitly warns users and the report sensitivity metadata enumerates these fields. No credential is written.

## Data written by this extension
- `snapshot` resource, retained for 90 days: fixed provider, collection and coverage times, independent usage/cost status, aggregate completion token/request counters, project/model usage breakdowns, currency totals, and project/line-item cost breakdowns. It contains operational and potentially internal workload metadata, but no Admin API key, API-key ID, user ID, or organization ID.
- Report JSON: dashboard bundle containing aggregate usage and billed costs, a provider-specific hashed bundle ID, and the local Swamp producer model name and model ID, with project, model, line-item, and producer identity fields listed in sensitivity metadata.
- Report Markdown: bundle title and state plus aggregate usage/cost section summaries; it contains no API credential or project/model/line-item breakdown identifiers.

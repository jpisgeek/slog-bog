# Secrets & Security Review
extension: @jpisgeek/video-pipeline            version: 2026.09.04.1
content-hash: 91a2f92b659c0c79ebff12b8a749f53b9b5d811149ebc3f4145fb2fccdcdf2c3
reviewer: gpt-daybreak-blue-latest   date: 2026-09-04
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| — | — | — | No findings. | — | — |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information — Reviewed every supplied source, comment, literal, fixture, test, example, manifest field, generated document, shared document/generator file, license, and prior hash-bound verdict. Found no credential, private infrastructure identifier, real local path, private host/domain, account identifier, IP address, secret-store item, control character, or hidden secret. The license identity, public package handle, and public repository URL are accepted published identities; all operational paths are explicit examples or runtime-created temporary paths.
2. Credential handling — The extension has no credential argument or secret-store access. Bridge stderr and malformed receipt bodies are suppressed from failures, schema errors do not echo payloads, and no secret-shaped field is written to resources. The configured trusted executable inherits the operator environment as explicitly documented; the adapter never serializes that environment.
3. Transport and network — The adapter and synthetic fixture make no network request and expose no URL, redirect, TLS, cleartext, or network timeout surface. Process timeout and caller cancellation are connected to the direct bridge command and are distinguished correctly; descendant cleanup is explicitly assigned to the trusted operator bridge.
4. Untrusted input — Fixed stage names, release and SHA-256 schemas, strict global and receipt schemas, stripped merged method extras, fixed argv without a shell, canonical workspace containment, absolute-path validation, ancestor symlink rejection, unique nonempty artifact validation, streamed byte-count/hash checks, output-root restrictions, dependency binding, video-hash membership, generic non-sensitive failures, and synthetic destination confinement were checked and clean. The trusted executable/bridge boundary is explicit and no unknown method property reaches argv.
5. Supply chain — `npm:zod@4` uses the sanctioned runtime major and `node:crypto` is built in. `jsr:@std/assert@1.0.19`, `jsr:@std/yaml@1.0.5`, and `jsr:@std/path@1.0.8` are exactly pinned. No other remote dependency, remote dynamic import, load-time fetch, `eval`, `new Function`, or untrusted deserialization into code was found.
6. Documentation matches code — The generated README now exposes all headings, tables, and the `sh`, `yaml`, and `text` code blocks as actual Markdown. Source-only installation and GitHub-only publication are consistent across the extension README, root README, variables, template, and generator. Type/version, five global arguments, seven methods, seven resources, dependency graph, fixed argv, timeout, receipt schema, streamed validation, trusted bridge boundary, data behavior, and synthetic-only example match the implementation. The previous FAIL remains clearly bound to its obsolete content hash.
7. Data written — Seven stage resources retain validated receipt metadata. No credential field exists. Absolute artifact paths are potentially identifying runtime metadata and the README explicitly tells users not to commit receipts or model instances; details are listed below.
Coverage — Reviewed all 11 manifest files plus shared `README.md`, `scripts/gen-readme.ts`, `templates/README.md.tmpl`, and the previous hash-bound FAIL verdict. The extension content hash independently reproduced as `91a2f92b659c0c79ebff12b8a749f53b9b5d811149ebc3f4145fb2fccdcdf2c3`; the supplied staged patch independently reproduced as SHA-256 `632161132652d293be652c30c8457279686c58454bc339813ba9187ab9cab4d2` across those 15 files.

## Data written by this extension
- `prepare`, `narrate`, `render`, `inspect`, `package`, `verify`, and `deliver` each write one receipt with `stage`, `release`, `inputHash`, `completedAt`, `artifacts`, `videoSha256`, and `metrics`.
- Each `artifacts` entry stores an absolute runtime path, positive byte count, and SHA-256. Absolute paths can disclose operator project and delivery layout and are personal/sensitive runtime metadata; the README tells users not to commit receipts, model instances, generated media, or private project sources.
- `metrics` stores nullable duration and clipped-sample counts, artifact count, and the bridge's verification boolean. No receipt schema field is intended to contain credentials, media contents, stderr, arbitrary diagnostics, or the inherited process environment.

# Secrets & Security Review
extension: @jpisgeek/video-pipeline            version: 2026.09.04.1
content-hash: ee26520e3b12409ecdc39f68c72ab2bdc99a76460663885d0c634e2352c3a238
reviewer: gpt-daybreak-blue-latest   date: 2026-09-04
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | video-pipeline/README.md:15 | The generated README has mismatched nested fences that render the install instructions and most of the generated model reference as code blocks instead of usable documentation. This hides headings, tables, and the example structure from readers. | The file opens a four-backtick fence at line 15, embeds ` ```sh ` at line 19, opens another four-backtick fence at line 25, and does not close that second fence until line 120. | Remove the unconditional outer fence around `{{install}}` in `templates/README.md.tmpl`, render the install variable as Markdown, regenerate the README, and visually or structurally verify that Install, Arguments, Methods, Data written, and Example are outside unintended fences. |
| 2 | block | README.md:38; video-pipeline/README.md:175 | The shared public documentation says every extension can be installed with a registry pull and that this source-only package reaches the registry, contradicting the package's stated GitHub-source-only distribution. Users following the root command cannot install this extension as documented. | `swamp extension pull @jpisgeek/<name>` and `before it reaches the registry` conflict with `no public registry upload is required`. | Make the root installation guidance direct readers to each extension's own install instructions or explicitly distinguish registry and source-only packages. Change the shared template's gate sentence to distribution-neutral publication wording, then regenerate affected READMEs. |
| 3 | fix | video-pipeline/video_pipeline.ts:222 | Artifact verification reads each complete artifact into memory. A normal large video, or a hostile stored receipt pointing to a large in-project file, can cause excessive memory use before size/hash validation completes. | `const bytes = await Deno.readFile(real);` | Hash through a bounded streaming reader while counting bytes and checking cancellation between chunks; keep the existing path checks before and after the read. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information — Reviewed source, comments, literals, fixtures, tests, examples, manifest, license, and all supplied prose. Found no credential, private infrastructure identifier, real local path, private host/domain, account identifier, IP address, control character, or hidden secret. The license identity, public package handle, and public repository URL are accepted published identities.
2. Credential handling — The extension has no credential argument or secret-store access. It does not write bridge stderr or malformed receipt bodies into errors or resources; see tests covering both failure paths.
3. Transport and network — The adapter and synthetic fixture make no network request and expose no URL, redirect, TLS, cleartext, or network timeout surface. Process timeout and caller cancellation are connected to the bridge command; descendant cleanup remains explicitly assigned to the trusted operator bridge.
4. Untrusted input — Fixed stage names, release and SHA-256 schemas, strict receipt schemas, fixed argv construction without a shell, canonical containment checks, ancestor symlink rejection, unique nonempty artifact validation, current byte/hash checks, output-root restrictions, dependency binding, video-hash membership, generic non-sensitive failures, and synthetic destination confinement were checked and clean. The trusted-bridge boundary is explicit.
5. Supply chain — `npm:zod@4` uses the sanctioned runtime major. `jsr:@std/assert@1.0.19`, `jsr:@std/yaml@1.0.5`, and `jsr:@std/path@1.0.8` are exactly pinned. No other remote dependency, remote dynamic import, load-time fetch, `eval`, or generated-code execution was found.
6. Documentation matches code — See findings 1 and 2. Apart from those publication blockers, the type/version, five global arguments, seven methods, seven resources, dependency graph, fixed argv, receipt schema, trusted bridge boundary, data behavior, and synthetic-only example match the implementation.
7. Data written — Seven stage resources retain validated receipt metadata. No credential field exists and runtime path privacy is explicitly documented; details are listed below.
Coverage — Reviewed all 11 manifest files plus shared `README.md`, `scripts/gen-readme.ts`, and `templates/README.md.tmpl`. The supplied staged patch was independently checked as SHA-256 `1e8ef229a666a2cb0ae235a778f83c0de5ec5e700a5ba68f0583fcad2fe54775` across those 14 files.

## Data written by this extension
- `prepare`, `narrate`, `render`, `inspect`, `package`, `verify`, and `deliver` each write one receipt with `stage`, `release`, `inputHash`, `completedAt`, `artifacts`, `videoSha256`, and `metrics`.
- Each `artifacts` entry stores an absolute runtime path, byte count, and SHA-256. Absolute paths can disclose operator project and delivery layout and are personal/sensitive runtime metadata; the README tells users not to commit receipts or model instances.
- `metrics` stores nullable duration and clipped-sample counts, artifact count, and the bridge's verification boolean. No receipt schema field is intended to contain credentials, media contents, stderr, or arbitrary bridge diagnostics.

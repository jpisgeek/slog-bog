# Secrets & Security Review
extension: @jpisgeek/dashboard-homelab            version: 2026.09.05.1
content-hash: 87bb8e66ae99fe55b518d22d92e43eac3fadbac3cf2a83feb911a7d831c01874
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all four published files. Found no credential, private infrastructure literal, real local path, private domain, account ID, secret-store item, control character, or hidden secret. Publisher/repository identities and vendor/product names are accepted; README examples are explicit placeholders.
2. Credential handling: the report accepts no credentials. Repository errors, JSON errors, schema issues, raw handle names, node transport errors, alarm prose, and formatted alert text are never emitted; they become fixed classifications and opaque digests. Source-controlled names pass a bounded structural allowlist or are withheld.
3. Transport and network: the report makes no network request and exposes no transport, redirect, TLS, or URL-fetch surface. It reads only execution-scoped Swamp data through the report context.
4. Untrusted input: reads stop at 5,000 handles and reject records over 1 MiB before decode. Collector records are schema-validated; duplicate rollups become unavailable; summary and record counts are reconciled; impossible Firewalla counts reject; missing device evidence is unmeasured rather than zero; failure and unauthorized states remain visible. Length-prefixed identities, provider-instance namespacing, SHA-256-derived opaque IDs, and occurrence suffixes prevent practical or equal-input record collisions. No source input reaches HTML, shell, SQL, or filesystem paths.
5. Supply chain: the only import is sanctioned runtime `npm:zod@4`; there are no dynamic imports, fetched code, evaluation, or deserialization into code. The inlined dashboard contract is byte-identical to the reviewed canonical copy.
6. Documentation matches code: verified supported collector types, scoped-handle behavior, 5,000-record and 1 MiB limits, rollup reconciliation, Firewalla filtered/unmeasured coverage, TrueNAS dismissal and severity behavior, stable opaque identities, allowlisted names, emitted-field inventory, source-only installation, and no-network claim.
7. Data written: see below. Operational infrastructure names and measurements can be personal or identifying; the README explicitly inventories them, all sections classify them operational, and redaction metadata records withheld free text. No credential is written.

## Data written by this extension
- Report JSON: one dashboard bundle for Netdata, TrueNAS, or Firewalla with a per-collector opaque namespace, safe producer name, health/coverage/freshness state, bounded operational metrics, facts, and exceptions. Depending on collector, it can contain allowlisted node, alarm, mount, pool, certificate, alert, machine, and network names plus usage, count, status, expiry, and device-coverage values. The raw Swamp model ID, raw data-handle names, repository errors, alarm prose, and formatted alert text are omitted.
- Report Markdown: bundle title and state, the collector section summary, coverage scope, and completeness state. It contains only normalized or allowlisted operational values and no credential or raw failure text.

# Secrets & Security Review
extension: @jpisgeek/lmstudio            version: 2026.09.05.1
content-hash: ff31e482365e54272921e0bc6a5e3df0357bbc615c6d0aa4c5b08e288dc4f7e1
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | `lmstudio/lmstudio_daemon.ts:410` | Published production code exports a setter for a mutable module-global command runner. Any importer can replace the runner, and later legitimate daemon observations in the same process will trust fabricated command output. The mutation also persists across calls until another caller resets it. | `export function setCommandRunnerForTest(runner?: CommandRunner)` | Remove the exported global mutation. Keep the production runner immutable and inject a runner only through a non-exported per-call test harness or a factory whose state cannot affect other instances. Add a regression proving one caller cannot alter a later production observation. |
| 2 | operator-decision | `lmstudio/lmstudio_endpoint.ts:39`, `lmstudio/lmstudio_probe.ts:124` | Both authenticated HTTP models permit cleartext `http://` endpoints. On that hop, an on-path party can read the bearer token and prompts/responses and can rewrite probe results. | `u.protocol === "http:" || u.protocol === "https:"` | Operator accepts the documented requirement that HTTP be used only inside an already encrypted transport, or requires HTTPS and gives up direct cleartext endpoint support. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all six published files; no credential literal, private operator path/host/account, control character, or hidden secret was found. Public publisher/repository/vendor identity and obviously synthetic examples are accepted.
2. Credential handling: API tokens are sensitive, sent only in Authorization headers, excluded from URLs/resources/tags, and redacted and echo-checked across response and error paths before storage or logging. Probe input is also sensitive and screened from errors. See finding 2 for transport exposure.
3. Transport and network: URLs reject userinfo/query/fragment and non-HTTP schemes, redirects are refused, response bodies are capped, request deadlines exist, and caller cancellation remains distinct from timeout or endpoint failure. Remote daemon mode restricts the cleartext CLI connection to a loopback address for an encrypted tunnel. See finding 2.
4. Untrusted input: endpoint responses and CLI output are bounded, screened, and schema-validated; missing measurements remain unknown rather than zero; model identifiers are bounded and screened; deterministic length-prefixed identities avoid separator collisions; no response text is rendered as HTML or passed to a shell. See finding 1.
5. Supply chain: only sanctioned runtime `npm:zod@4` is imported; no dynamic code, remote dependency, eval, or fetched executable exists. The intended subprocess path uses fixed argv with a cleared environment, bounded output, timeout, cancellation, and descendant-pipe cleanup. See finding 1 for the exported runner replacement.
6. Documentation matches code: verified all three models, methods, arguments, resource schemas, endpoint and daemon transport rules, response bounds, cancellation, error/unknown semantics, identity rules, and written-data disclosure. The README describes the intended argv-only production runner but does not disclose the exported global replacement in finding 1.
7. Data written: see below. The README identifies endpoint/model/probe/daemon operational data and token-usage sensitivity. No bearer token or prompt/input text is written.

## Data written by this extension
- Infinite `models` resources: endpoint base URL, served model identifiers and count, and sync time.
- Infinite `health` resources: endpoint URL, reachability, authorization state when determined, HTTP status, latency, bounded fixed-class error details, and check time.
- Infinite `embeddingProbe` resources: endpoint/model identity, requested and measured dimensions, vector validity/norm, latency, HTTP status, fixed-class error details, and check time.
- Infinite `completionProbe` and `capabilityProbe` resources: model, latency/status, finish and optional token/reasoning counts, content-shape and capability booleans, truncation/context diagnostics, fixed-class errors, and check time. Prompt and generated text are not stored.
- Infinite `daemon` resources: local or loopback-tunnel daemon target, reachability, CLI status/version, loaded model count, fixed-class error details, and check time.
- Infinite `daemonModel` resources: daemon target plus bounded model identifier, type, state, architecture, quantization, context length, and memory size when reported.

# Secrets & Security Review
extension: @jpisgeek/caddy            version: 2026.09.04.1
content-hash: 10a2bd0fa04efe0eb7cd4ad491568d3af88f7a22175fffbe3845318079bb7f01
reviewer: qwen3.6-35b-a3b (LM Studio local inference, Anvil)   date: 2026-09-10
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|

severity: block (must fix before publish) · fix (should fix; not a publish blocker on its own) · note (informational) · operator-decision (trade-off the operator must rule on)
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: No hardcoded credentials, real hostnames, IPs, or operator-specific paths. Configuration defaults (`/etc/caddy/Caddyfile`, `/usr/bin/caddy`, `/usr/bin/systemctl`) are standard Linux paths. Handles (`sshModel`, `host`) are user-supplied.
2. Credential handling: Delegates all authentication to the pre-existing `@swamp/ssh` model. No secrets are passed, logged, or exposed in error messages or output.
3. Transport and network: Uses Swamp CLI over SSH. No direct network calls. Explicit timeouts are enforced on both the CLI invocation and internal subprocess calls.
4. Untrusted input: Strict Zod schemas validate all inputs. The Python driver validates paths, symlinks, permissions, CIDRs, and upstream format. Shell commands are executed via argument lists to prevent injection. Failures throw errors rather than returning a healthy state.
5. Supply chain: `npm:zod@4` is the sanctioned runtime dependency. No other external imports or dynamic fetches exist.
6. Documentation matches code: README arguments, methods, data schema, example config, and caveats align exactly with the TypeScript and Python implementation.
7. Data written: Extension writes `operation` resource containing request metadata, digests, validation status, and a remote backup path. No secrets or personal data are exposed in the output.

## Data written by this extension
- `operation` resource: `requestId`, `action`, `beforeSha256`, `afterSha256`, `changed`, `validated`, `serviceActive`, `backupPath`, `route`. (Contains a remote filesystem path to a backup file, disclosed for auditability but safely scoped to the remote host.)

---
## Gate provenance (appended by the operator's gate, not the reviewer)
Produced by **qwen3.6-35b-a3b** via LM Studio local inference on Anvil (jp-mac-studio), fed the exact published surface (caddy.ts, README.md, LICENSE, manifest.yaml) **plus the bundled-inline driver.ts** — which swamp inlines into the model at publish but which `content-hash.ts` does not include in the hash above. The rubric was `review/secrets-security-pass.md`. The configured cross-vendor reviewer (`gpt-daybreak-blue-latest` via codex) was unavailable — the ChatGPT subscription is capped until 2026-09-15 — so a local model was used on Jason's explicit authorization (2026-09-10). The `reviewer:` line records the actual model; the model self-identified as "assistant" because it cannot know its own LM Studio deployment identifier. A cross-vendor re-review when codex returns would strengthen this verdict.

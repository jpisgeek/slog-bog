# Secrets & Security Review
extension: @jpisgeek/firewalla            version: 2026.09.05.1
content-hash: 434703641918aff53c8c6ab9c7647efb5ceb43eb90231561c752a87ae978c5bd
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | operator-decision | `firewalla/README.md:122` | A full fetch judged unrepresentative refuses pruning but still overwrites the inventory rollup with its reduced counts and a fresh timestamp, without a persisted representative/degraded marker. Consumers reading only that resource cannot distinguish suspicious coverage from a real decommission. | `a run that refuses to prune still SUCCEEDS and still overwrites the inventory roll-up with the reduced counts it saw` | Operator accepts the documented freshest-observation tradeoff, or adds a persisted coverage/representativeness field and teaches consumers to require it. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all four published files; no credential literal, private operator path/host/account, control character, or hidden secret was found. Public publisher/repository/vendor identity and placeholder examples are accepted.
2. Credential handling: the MSP token is sensitive, used only in the Authorization header, absent from URLs/resources/tags, redacted from bounded errors, and checked against all decoded response fields and non-sensitive configuration before logs, hashing, or writes. Exact, folded, and invisibly split echoes abort.
3. Transport and network: the token destination is restricted to a bare HTTPS subdomain of `firewalla.net` with no userinfo/path/query/fragment/port. Redirects are refused, retries are bounded and Retry-After aware, and caller cancellation/whole-call timeout remain distinct through request, wait, and body reads.
4. Untrusted input: response fields are explicitly projected and credential-scanned; unknown fields are ignored only after the whole payload scan. Counts preserve unknown versus zero, resource identities use length-prefixed tuples and 128 bits of SHA-256, normalized configuration collisions are rejected, grouping is order independent, and prune/exclusion guards prevent filtered or implausible absence from deleting records. See finding 1.
5. Supply chain: only sanctioned runtime `npm:zod@4` is imported; no dynamic code, shell, subprocess, or fetched executable exists.
6. Documentation matches code: verified arguments, one method, three resources, MSP endpoint/transport, retries, bounds, device/machine grouping, exclusions, unknown-field handling, deterministic identities, prune policy, and written-data disclosure. See finding 1.
7. Data written: see below. This is explicitly personal network inventory and the README tells users to protect it. No token is written.

## Data written by this extension
- Infinite `device` resources: device/MSP identifiers, names, IP/MAC/vendor/type/network, presence, reservation/router/firewall flags when known, traffic totals when known, tier, SSH candidacy, and exclusion state.
- Infinite `machine` resources: collapsed machine name/address/type/vendor/tier, presence, networks and interfaces, SSH candidacy, and dependency.
- Infinite `inventory` rollup: MSP tenant hostname, device baselines/counts, excluded networks, machines/SSH candidates, network names, type histogram, and sync time. These fields identify the operator's network and tenant.

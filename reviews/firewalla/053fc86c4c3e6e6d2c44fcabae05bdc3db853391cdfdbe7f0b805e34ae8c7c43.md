# Secrets & Security Review
extension: firewalla            version: 2026.08.22.2
content-hash: 053fc86c4c3e6e6d2c44fcabae05bdc3db853391cdfdbe7f0b805e34ae8c7c43
reviewer: GPT-5   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | firewalla/firewalla.ts:1147-1190 | Missing security-relevant booleans are silently treated as false. In particular, an omitted or renamed `isFirewalla` field can classify the firewall as an SSH candidate. `ipReserved` and `isRouter` are also recorded as known-false when actually absent. | `const isFirewalla = optBool(raw.isFirewalla) ?? false` and `sshCandidate: tier === "deep" && !isFirewalla` | Validate required booleans. Preserve unknown values or reject affected records; an unknown `isFirewalla` value must conservatively prevent SSH candidacy. |
| 2 | block | firewalla/firewalla.ts:1270-1335 | Same-named machine identity is order-dependent. The first matching device receives the name-only key, while later devices receive MAC/ID keys. API ordering changes can therefore swap resource identities. For two multi-homed hosts sharing interface names, each interface of the later host can become a separate machine instead of being collapsed. | `const mKey = nameTaken ? identityTuple([stripped, device.mac, device.id]) : primaryKey` | Build machine groups in a deterministic second pass. Give every colliding host—not merely later entries—a stable identity, and do not claim NIC deduplication where the API provides no reliable host association. |
| 3 | operator-decision | firewalla/firewalla.ts:545-620 | MSP responses deliberately receive field-by-field tolerant parsing rather than strict schema validation; added fields are ignored and malformed array entries are skipped. This permits partial inventories after incompatible response changes. | `Deliberately NOT a z.object({...}).strict()` | The operator must explicitly accept partial inventory under schema drift, or require strict response schemas and fail/degrade the run when entries do not validate. |
| 4 | operator-decision | firewalla/firewalla.ts:1147-1159 | Missing `online` is deliberately converted to `false`. This avoids false-green health but can make an API schema change report the fleet offline. | `const isOnline = onlineFlag ?? false` | Accept the documented false-offline behavior, or represent online state as unknown and require consumers to handle it. |
| 5 | operator-decision | firewalla/firewalla.ts:1463-1518 | An empty, malformed, or suspiciously shrunken response still overwrites the inventory roll-up without a degraded/representative marker. Consumers cannot distinguish failure from genuine decommissioning. | `The inventory resource carries no representative-or-degraded marker` | Accept that consumers must inspect logs or historical totals, or add a required representative/degraded field and update downstream consumers. |
| 6 | operator-decision | firewalla/firewalla.ts:477-485,1260-1335 | NIC collapsing is name-suffix heuristic. Distinct hosts named, for example, `<base>-eth` and `<base>-wifi` can be merged despite having unrelated identities. | `return suffixes.some((sfx) => sfx.toLowerCase() === tail) ? name.slice(0, idx) : name` | Accept possible false merges, require an explicit host mapping, or stop automatically merging devices when no stable shared host identifier exists. |
| 7 | fix | firewalla/firewalla.ts:12 | Published source documentation still says instance names use FNV-1a, while implementation uses truncated SHA-256. | `device-<slug>-<fnv1a>` | Update the module comment to the implemented SHA-256-based format. |
| 8 | fix | firewalla/README.md:105 | README claims resource names can never collide, but the implementation truncates SHA-256 to 128 bits and explicitly acknowledges a nonzero collision probability. | `can never collide onto one name` | Describe the names as collision-resistant rather than collision-impossible. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: no credentials, private infrastructure identifiers, non-example secret-store names, local paths, or hidden control characters found; accepted publisher, package, repository, and vendor identities were not treated as findings.
2. Credential handling: token is marked sensitive, sent only in the authorization header, redacted from error paths, and never written to resources, filenames, or URLs.
3. Transport and network: HTTPS is forced, vendor-domain validation is present, userinfo/ports/paths are rejected, redirects fail, TLS verification is not disabled, and timeout/caller signals cover fetches, retries, and body reads.
4. Untrusted input: see findings 1–6; no HTML, shell, SQL, executable deserialization, or unsafe URL-scheme sinks found.
5. Supply chain: only sanctioned `npm:zod@4` is imported; no unpinned dependencies, dynamic remote imports, fetched execution, `eval`, or `new Function` found.
6. Documentation matches code: see findings 2, 7, and 8; manifest/model/README names, versions, arguments, methods, resources, and principal failure behavior otherwise match.
7. Data written: personal network inventory is written, but README explicitly discloses it; no credential or token write path was found.

## Data written by this extension

- `device`: device and group IDs, device name, IP and MAC addresses, MAC vendor, device type, network name, online/reservation/router/firewall state, traffic totals, tier, SSH candidacy, and exclusion state. This is personal/sensitive network inventory.
- `machine`: machine name, primary IP, device/vendor classification, online and SSH state, network names, dependency name, and interface names, IPs, MACs, networks, and states. This is personal/sensitive host inventory.
- `inventory`: MSP tenant hostname, device/machine/state counts, excluded and observed network names, device-type histogram, and synchronization timestamp. The tenant hostname and network names identify the operator’s environment.
- No access token or other credential is written.
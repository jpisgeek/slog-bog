# Secrets & Security Review
extension: @jpisgeek/truenas            version: 2026.09.05.1
content-hash: 1c1509b3550ab462c0f37f42b2b42417b67b3f7442edddfbfa8442730f42a676
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | operator-decision | `truenas/README.md:277` | `allowInsecureHttp` permits the API key and inventory to travel over cleartext WebSocket. Anyone on the path can capture a live key, read the inventory, or impersonate the NAS. | `allowInsecureHttp: true permits http:// -> ws:// with the key in cleartext on every call` | Operator accepts the documented loopback/VPN-only exception, or removes the override and requires authenticated TLS. |
| 2 | operator-decision | `truenas/README.md:268` | The destination allowlist is optional. If `baseUrl` is supplied indirectly and changes while `allowedHosts` is empty, the extension sends the API key to the newly named host. | `Left unset, the key goes to whatever host baseUrl names.` | Operator accepts literal URL validation as sufficient, or requires `allowedHosts` and gives up the no-duplicate-pin configuration. |
| 3 | operator-decision | `truenas/README.md:298` | Bounded, screened remote diagnostic and alert prose is retained in logs and permanent resources. It can disclose hostnames, addresses, users, share/dataset paths, and serials, or carry attacker-chosen misleading prose, although controls and API-key forms are removed. | `a bounded, screened fragment of whatever the box said still reaches your logs and your alert records` | Operator accepts the documented operational-inventory sensitivity, or replaces remote prose with fixed classes and gives up actionable alert detail. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all four published files; no live credential, private operator host/path/account, control character, or hidden infrastructure identifier was found. Public publisher/repository/vendor identity and synthetic examples are accepted.
2. Credential handling: `apiKey` and `baseUrl` are sensitive; the key grammar is bounded, it is sent only as the authentication RPC parameter, authentication prose is dropped, other remote strings are screened for raw, escaped, split and folded key forms, generated identities are checked before hashing/writing, and neither URL nor key is logged or stored. See findings 1 and 2 for destination exposure.
3. Transport and network: HTTPS/WSS is the default, redirects and compression are disabled in the exact-pinned WebSocket client, the socket-reported destination is rechecked before authentication, userinfo/path/query/fragment are refused, connection and call deadlines exist, and cancellation closes the socket and prevents later sends. See findings 1 and 2.
4. Untrusted input: JSON-RPC envelopes, response collections, relied-on fields, derived resources and duplicate identities are validated before writes; response and collection sizes are capped before expensive work; unknown measurements remain explicit; length-prefixed 128-bit SHA-256 identities resist collision; malformed/partial results cannot trigger pruning; and generation markers expose datastore-interrupted writes. See finding 3 for deliberately retained prose.
5. Supply chain: `npm:ws@8.21.0` is exact-pinned and configured with bounded payloads, no redirects and no compression; `npm:zod@4` is the sanctioned runtime exception. No dynamic code, shell, subprocess, eval, or fetched executable exists.
6. Documentation matches code: verified one discover method, arguments, six resources, five concurrent read-only RPCs, authentication and destination checks, bounds, unknown/degraded semantics, generation completion, identities, prune protection, and data disclosure. See findings 1-3.
7. Data written: see below. The README clearly classifies it as sensitive infrastructure inventory. No API key is written.

## Data written by this extension
- Infinite `system` resource: hostname, TrueNAS version/model, CPU, memory, uptime, load, measurement-known state, and generation ID.
- Infinite `pool` resources: pool names, health/status, capacity and fragmentation measurements with known-state markers, and generation ID.
- Infinite `disk` resources: disk name, serial, model, size, type and pool membership with known-state markers, and generation ID.
- Infinite `alert` resources: alert identifier, class, severity, bounded screened alert prose, dismissal/silence and content-known state, and generation ID.
- Infinite `certificate` resources: certificate names/common names, canonical expiry, days remaining, expiry state and generation ID. Names commonly disclose internal DNS names.
- Infinite `summary` resource: host/version, generation completeness, object and health counts, unknown/degraded indicators, and sync time.

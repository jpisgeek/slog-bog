# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 8d81bbf8f91b011af69e469d6e5347f10117db32cea954d94702a83b485e2d4f
reviewer: GPT-5 Codex   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:379-396,760-821,1084-1110 | The `errorDetail` opt-in persists untrusted stderr and exception text. Heuristic screening cannot guarantee removal of credentials, hostnames, account names, or private paths. | `errorDetail: g.errorDetail ? (detail ?? null) : null` | Remove `errorDetail` and persist only closed-set error codes. |
| 2 | block | mise/mise.ts:1035-1070,1250-1410 | Arbitrary remote names, versions, and paths are persisted after denylist screening. Secrets disguised as ordinary strings or URL path components can pass; the README explicitly acknowledges this. | `for (const re of CREDENTIAL_SHAPES) if (re.test(s)) return null;` | Apply strict field-specific allowlists and length limits. Omit or irreversibly hash raw install, source, and config paths that cannot be proven safe. |
| 3 | block | mise/mise.ts:1200-1242 | Untrusted response and stored-record schemas explicitly accept unknown fields, contrary to the required strict-schema boundary. | `}).loose();` | Use strict schemas and explicitly version every accepted upstream response shape. |
| 4 | block | mise/mise.ts:1340-1380,2040-2100,2350-2400 | A malformed `outdated` entry is dropped and subsequently represented as `outdated: false`. Parser drops also leave summary totals numeric, allowing incomplete data to read as healthy. | `outdated === null ? null : Object.hasOwn(outdated, r.tool)` | Track per-entry unknown states, write `null` plus `unmeasured`, and null affected summary totals whenever relevant entries are dropped. |
| 5 | note | mise/README.md:198-215 | Resource data, names, and tags disclose fleet labels, account-derived paths, project paths, internal source locations, and toolchain fingerprints. The README does clearly warn users. | `Treat the datastore, and the sweep logs alongside it, as infrastructure-sensitive.` | Retain the warning, minimize raw paths, and require a private access-controlled datastore. |

## Checked and clean
1. Published surface: checked credentials, identities, domains, addresses, local paths, secret-store names, and hidden controls; found only accepted publisher/repository identity and generic examples.
2. Credential handling: see findings 1–2; no secret-bearing arguments, URL userinfo, or direct disk persistence otherwise found.
3. Transport and network: SSH requires strict host-key checking, disables forwarding, has deadlines, and honors caller cancellation; no cleartext or TLS-disable option found.
4. Untrusted input: see findings 3–4; command construction rejects shell metacharacters, and no HTML, SQL, or API-derived file operation was found.
5. Supply chain: only sanctioned `npm:zod@4` is imported; no undeclared dependency, dynamic remote import, `eval`, or executable deserialization found.
6. Documentation: checked model/version, arguments, method count, resources, naming, failure behavior, and example configuration; no additional drift found.
7. Data written: see findings 1–2 and 5.

## Data written by this extension
- `node`: host label, measurement state, failure codes, optional sensitive error detail, mise version, measured directory, counts, and drift.
- `tool`: host label, tool and version details, install path, source type/path, state, and drift. Paths and sources may identify accounts, projects, repositories, or internal domains.
- `config`: host label, config-file path, trust state, and declared/effective tool lists.
- `summary`: aggregate fleet counts and sweep timestamp; no direct personal field.
- Resource names and tags additionally expose readable host labels, tool names, config-path fragments, transport, and state.
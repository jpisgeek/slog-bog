# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: ef3f007fda78291bf4600fbee3b67f3492fcf45c27307f29b233f7a6951ecdb5
reviewer: GPT-5   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:CREDENTIAL_SHAPES | The credential filter permits valid authentication credentials in host-controlled resource fields, names, and tags. Short bearer tokens and schemes such as Basic are not rejected. | `\b(?:bearer\|authorization)\s+...{12,}` | Reject every supported authorization scheme regardless of credential length, including Basic, Digest, Negotiate, and Authorization-header forms; add persistence tests for each. |
| 2 | block | mise/README.md:Caveats; mise/mise.ts:holdsRecord | The README promises a full sweep deletes every unwritten record, but ambiguous node prefixes are always retained, even when every matching node was measured. Stale infrastructure records can therefore persist contrary to the documented retention behavior. | `if (matches.length > 1) ... return true` | Make resource names unambiguously attributable to a node, or accurately document the retention exception and its stale-data consequence. |
| 3 | block | mise/mise.ts:StoredRecordSchema | Datastore-supplied resource names need only be nonempty before being written to logs. Control and format characters can therefore reach log output despite the model's stated printable-text boundary. | `z.object({ name: z.string().min(1) }).loose()` | Validate stored names against a bounded printable resource-name schema before logging or deletion. |
| 4 | operator-decision | mise/mise.ts:GlobalArgsSchema.errorDetail | The opt-in diagnostic feature deliberately persists host-controlled error prose that screening cannot guarantee is free of credentials or infrastructure identifiers. | `host error text routinely carries credential-bearing URLs, tokens, private hostnames and home paths` | Remove `errorDetail` and retain closed-set error codes only. Keeping it accepts possible durable disclosure in private datastores. |
| 5 | operator-decision | mise/mise.ts:LsEntrySchema–OutdatedEntrySchema | Remote response schemas deliberately accept undeclared fields, contrary to the strict-response requirement. | `.loose()` | Use strict schemas and explicitly update them for upstream changes. Keeping this accepts unknown response members to preserve forward compatibility. |
| 6 | operator-decision | mise/README.md:Security | The extension deliberately stores infrastructure-identifying inventory, including host labels, account-bearing paths, private repository locations, and internal domains. The README clearly warns that the datastore and logs are infrastructure-sensitive. | `Install and config paths embed the remote account's home directory and real project directory names.` | Remove or irreversibly pseudonymize these fields. Keeping them accepts an infrastructure-sensitive datastore as a core feature. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Checked all published code, comments, examples, README, manifest, and LICENSE for literal secrets, unaccepted identities, real infrastructure, private addresses, local operator paths, and hidden controls; see findings 1, 4, and 6.
2. Checked schemas, logs, errors, resource attributes, names, tags, URLs, and persistence paths for credential exposure; see findings 1, 3, and 4.
3. Checked SSH transport, host-key verification, forwarding controls, schemes, timeouts, and caller cancellation; no cleartext mode, TLS bypass, redirect, or cancellation defect found.
4. Checked remote responses, shell construction, paths, rendering, identities, collisions, and failure-as-health behavior; see findings 2, 3, and 5.
5. Checked every import and dependency: only sanctioned `npm:zod@4` is imported; no dynamic loading, fetched execution, `eval`, or `new Function` found.
6. Checked README types, arguments, methods, resources, examples, failure behavior, naming, and pruning against code; see finding 2.
7. Checked every resource field, name, and tag written; see findings 1, 4, and 6.

## Data written by this extension
- `node`: host label, measurement and degradation state, failed probes, failure codes, transport, optional error detail, mise version, measured directory, counts, and drift. Host labels, directories, and optional error details are infrastructure-sensitive; error details may contain secrets.
- `tool`: host and tool names, requested/resolved/latest versions, install and source paths, source type, state flags, and drift. Paths and repository URLs may identify accounts, private projects, or internal domains; finding 1 permits some credentials through.
- `config`: host label, config path, trust state, and declared/effective tool lists. Paths and labels are infrastructure-sensitive.
- `summary`: aggregate fleet counts and sweep timestamp; no direct personal fields.
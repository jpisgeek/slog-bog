# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 8590c8eb886701a00dab8b488ec5bccf9c1fb14cc79561dedd27d017ac0f46ed
reviewer: GPT-5   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:LsEntrySchema, discover/expectFail | An installed, active tool whose response omits `version` can be reported with no expected-version drift and no unmeasured state. Absence therefore looks healthy. | `version: z.string().optional()` and `r.resolvedVersion !== null && !satisfiesExpect(...)` | Require a resolved version when `installed` or `active` is true, or mark the tool unmeasured and the node degraded when it is absent. |
| 2 | block | mise/mise.ts:USERINFO_NO_SCHEME | Credential screening misses scp-style and scheme-relative locations using bracketed IPv6. A value such as `user:password@[fd00::1]:repo` can be persisted with its username, password, and private address intact. | The host portion only accepts `@[A-Za-z0-9.-]+:` or `@[A-Za-z0-9.-]+`, excluding `[IPv6]`. | Recognize and reject bracketed IPv6 userinfo forms, with tests covering scp-style and scheme-relative URLs containing usernames and passwords. |
| 3 | block | mise/mise.ts:GlobalArgsSchema, nodePrefix, discover/prefixHeld | Distinct node names can normalize to the same prune prefix. Their hold states overwrite each other, so a failed host's retained records can be deleted or a measured host's stale records can survive. This contradicts the documented pruning guarantees. | Raw names alone are unique, while `prefixHeld.set(nodePrefix("tool", n.name), held)` keys by `slugPart(node)`; names such as `web` and `WEB` share a key. | Reject duplicate normalized node identities or redesign pruning to associate records with an unambiguous hashed/raw node identity. |
| 4 | operator-decision | mise/README.md:errorDetail; mise/mise.ts:safeDetail | The opt-in `errorDetail` feature durably stores host-controlled free text. Its heuristic screening cannot guarantee removal of cookies, session identifiers, opaque tokens, or secrets shaped like ordinary text. | `Screening is a set of shape rules, not a proof` and `g.errorDetail ? (detail ?? null) : null` | Remove free-text persistence for a secret-safe guarantee, or explicitly accept that enabling it can store credentials and infrastructure details in the datastore. |
| 5 | operator-decision | mise/mise.ts:LsEntrySchema–OutdatedEntrySchema | API response schemas deliberately accept unknown fields with `.loose()`, contrary to the strict-schema gate. Known fields are validated, but unexpected upstream structure does not fail the response. | `Unknown fields are tolerated on purpose` and four schemas ending in `.loose()` | Use strict schemas, or explicitly accept the forward-compatibility trade-off and the reduced ability to detect upstream schema drift. |
| 6 | note | mise/README.md:Security | The extension intentionally writes infrastructure-identifying inventory, including host labels, home/project/config paths, tool installation paths, and private source locations. The README clearly discloses this and tells users to treat the datastore and logs as infrastructure-sensitive. | `Install and config paths embed the remote account's home directory and real project directory names.` | No corrective code change is required for the disclosed core purpose; preserve the warning and restrict datastore and log access. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Published surface: no embedded credentials, real private infrastructure identifiers, non-placeholder addresses, or hidden control bytes found; accepted publisher, package, repository, vendor, and LICENSE identities were excluded.
2. Credential handling: no secret-bearing configuration arguments or disk persistence found; see findings 2 and 4 for persistence-screening weaknesses.
3. Transport and network: SSH is encrypted, strict host-key checking and forwarding restrictions are explicit, calls are bounded, and caller cancellation is honored.
4. Untrusted input: HTML, SQL, and dynamic shell construction were absent; see findings 1, 3, and 5.
5. Supply chain: only sanctioned `npm:zod@4` is imported; no undeclared dependencies, dynamic remote imports, fetched execution, `eval`, or `new Function` found.
6. Documentation: method, arguments, resource fields, examples, and normal behavior otherwise match code; see finding 3 for the pruning-guarantee mismatch.
7. Data written: see findings 4 and 6; no additional undocumented resource kinds or fields found.

## Data written by this extension
- `node`: host label, transport, measurement/degradation state, failure codes, optional potentially sensitive `errorDetail`, mise version, measured directory path, counts, and drift.
- `tool`: host label, tool and version data, installation path, source type/path, state, and drift; paths and sources may identify accounts, private repositories, or internal domains.
- `config`: host label, config-file path, trust state, and declared/effective tool lists.
- `summary`: fleet counts, degradation totals, drift totals, and sweep timestamp.
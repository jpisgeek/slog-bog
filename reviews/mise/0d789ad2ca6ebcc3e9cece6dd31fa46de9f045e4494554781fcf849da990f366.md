# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 0d789ad2ca6ebcc3e9cece6dd31fa46de9f045e4494554781fcf849da990f366
reviewer: GPT-5 Codex   date: 2026-08-27
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:GlobalArgsSchema.errorDetail, runMise catch, worker catch | The opt-in error-detail path durably stores untrusted exception and host text that may contain credentials or private infrastructure. One catch bypasses even the limited screening. | `errorDetail: g.errorDetail ? printableRemoteText(...).slice(0, 160) : null` | Remove `errorDetail` and persist only closed-set error codes. Never store or log raw host or exception text. |
| 2 | block | mise/mise.ts:CREDENTIAL_SHAPES, safeRemoteString, safeDetail | Credential screening is a small denylist and is readily bypassed. Bare API keys, `token=...`, punctuation-wrapped credential URLs, URL-path secrets, and split PEM markers can reach resource fields. | `const CREDENTIAL_SHAPES` contains only bearer text and a private-key marker; URL checks require `scheme://` at the start | Positively validate each persisted field. Remove or structurally reduce arbitrary paths and source strings instead of attempting universal secret detection. |
| 3 | block | mise/mise.ts:LsEntrySchema, ConfigEntrySchema, OutdatedEntrySchema | Remote responses explicitly accept unknown properties, contrary to the required strict boundary. | `.loose()` | Use strict schemas and treat unexpected response shapes as degraded or unmeasured. |
| 4 | block | mise/mise.ts:LsEntrySchema, parseLsCurrent | Required measurement fields are optional. An empty object becomes a measured tool with `installed: false`, `active: false`, null versions, and no dropped-entry warning. | `installed: z.boolean().optional()` and `installed: e.installed === true` | Require fields according to explicit installed/uninstalled response variants; reject and count incomplete rows. |
| 5 | block | mise/mise.ts:summary write | Failed config or outdated probes still produce summary values of zero, disguising missing measurements as clean aggregate counts. | `outdated: count("outdated")` and `configsNotInEffect: configStates.filter(...).length` | Make affected summary counts nullable/unknown when any contributing probe failed, or withhold the summary until complete. |
| 6 | block | mise/mise.ts:boundedDecode, runMise | The advertised output ceiling is applied only after `Deno.Command.output()` has buffered the entire process output, so a hostile host can exhaust memory before truncation occurs. | `const out = await cmd.output()` followed by `boundedDecode(out.stdout)` | Stream stdout and stderr with byte limits and terminate the child immediately when a limit is exceeded. |
| 7 | block | mise/mise.ts:discover.execute, prune query | Runtime and datastore boundaries use `any` and a blind cast; malformed repository data is neither validated nor safely rejected before deletion logic uses it. | `ctx: any` and `existing as { name: string }[]` | Define the runtime context type and strictly validate repository results before computing or performing deletions. |
| 8 | block | mise/mise.ts:parseVersion | Version validation is not anchored, so malformed text such as `1evil` is accepted as a valid version and can prevent the node being marked degraded. | `/^v?\d+(\.\d+)*/.test(first)` | Anchor the complete token and validate the exact supported version grammar. |
| 9 | fix | mise/README.md:Security; mise/mise.ts:unparseable ls handling | Documentation says the stored error is always a closed-set classification code, but unparseable output stores a prose sentence in `error`. | `mise exited zero without the JSON object it promises` | Add and store a fixed `unparseable` code, or accurately document the field contract. |
| 10 | note | mise/README.md:Data written, Security | Runtime records contain infrastructure-identifying data, including host labels, home/project paths and private source locations. The README does explicitly disclose this exposure. | `Install and config paths embed the remote account's home directory and real project directory names.` | No disclosure fix required; retain the warning and minimize fields where practical. |

## Checked and clean
Section 1 — Checked all published files for embedded credentials, operator infrastructure, non-example addresses, local paths and hidden controls; only the explicitly accepted publisher, scope, repository and product identities were present.
Section 2 — See findings 1 and 2; no credential-bearing schema arguments or secret-store references were found.
Section 3 — SSH is encrypted, strict host-key checking is forced, process deadlines exist, and caller cancellation is distinguished; no cleartext transport, TLS bypass, URL scheme or redirect path exists.
Section 4 — See findings 3–8; shell-bound `dir` and `misePath` are constrained, SSH destination values remain argv elements, HTML/SQL interpolation was not found, and hashed resource identities resist collisions.
Section 5 — Only sanctioned `npm:zod@4` is imported; no undeclared dependency, remote dynamic import, fetched execution, `eval`, or `new Function` was found.
Section 6 — See finding 9; manifest name/version, method, arguments, resources and example configuration otherwise match the implementation.
Section 7 — See findings 1, 2 and 10; the README enumerates the infrastructure-sensitive data, but secret-bearing free text can still be written.

## Data written by this extension
- `node`: host label, measurement state, failure details, transport, mise version, measured directory, counts and drift. `dir` is infrastructure-identifying; `errorDetail` may contain secrets.
- `tool`: host label, tool and version data, install path, source type/path and drift. Paths and source locations may expose usernames, internal domains, account identifiers or private repositories.
- `config`: host label, config-file path, trust state and tool lists. Paths may expose usernames and private project structure.
- `summary`: aggregate fleet counts and sweep timestamp; no direct personal fields.
- Resource names and tags additionally expose host labels, tool names, drift and transport metadata.
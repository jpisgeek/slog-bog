# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 6962a499e5b6e2ae4ad8e0e58a802da3ff09b023ce87d6dd7e279c8b941e554e
reviewer: GPT-5 (Codex)   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:safeDetail(), runMise(), execute() | The `errorDetail` option can durably store secrets from host errors. Its token-by-token screening misses spaced assignments such as `password = swordfish`, because no individual token matches the assignment detector. | `g.errorDetail ? (detail ?? null) : null` | Remove `errorDetail` and never persist or log host-controlled error text. Retain only closed-set error codes. |
| 2 | block | mise/mise.ts:CREDENTIAL_SHAPES, safeRemoteString() | Default resource fields, names, and tags can contain secrets that evade the heuristic denylist. All-uppercase credentials, short bare tokens, and credentials placed in URL paths pass unchanged. | `for (const re of CREDENTIAL_SHAPES) if (re.test(s)) return null; return s;` | Use strict field-specific allowlists and omit remote fields that cannot be safely bounded. Do not place arbitrary remote strings in resource names or tags. |
| 3 | block | mise/mise.ts:LsEntrySchema–OutdatedEntrySchema | Host responses are explicitly accepted with unknown fields, contrary to the required strict-response boundary. | `}).loose();` | Make every response schema strict. Treat unexpected fields or shapes as rejected input and mark the relevant probe degraded or unmeasured. |
| 4 | block | mise/mise.ts:sshArgs() | SSH inherits ambient forwarding configuration. If `ForwardAgent` is enabled in the user's SSH configuration, this supposedly read-only sweep can expose the authentication agent to a remote host. | `"BatchMode=yes", "StrictHostKeyChecking=yes"` | Pass `ForwardAgent=no`, `ClearAllForwardings=yes`, and `PermitLocalCommand=no` explicitly so ambient SSH configuration cannot add credential exposure or side effects. |

## Checked and clean
Section 1 — Checked every published file for embedded credentials, private infrastructure identifiers, real IPs, local personal paths, secret-store names, and control bytes; clean apart from the expressly accepted publisher, package, repository, and product identities.
Section 2 — See findings 1, 2, and 4.
Section 3 — See finding 4. SSH encryption, strict host-key checking, bounded output, timeouts, and caller cancellation were otherwise present.
Section 4 — See findings 2 and 3. Command construction and collision-resistant resource identity were otherwise clean.
Section 5 — Imports are limited to sanctioned `npm:zod@4`; no dynamic remote loading, undeclared dependency, evaluation, or code deserialisation was found.
Section 6 — README method, arguments, resource fields, naming, failure behaviour, and example configuration match the code.
Section 7 — See findings 1 and 2. The README does disclose that stored paths, host labels, inventories, and repository locations are infrastructure-sensitive.

## Data written by this extension
- `node`: host label, transport, measurement and failure state, mise version, measured directory, drift counts, and optional `errorDetail`; labels and directories identify infrastructure, and `errorDetail` can contain secrets.
- `tool`: host and tool names, requested/resolved/latest versions, install path, source type/path, state, and drift; paths may expose usernames, private projects, repositories, domains, or credentials.
- `config`: host label, configuration path, trust state, and declared/effective tool inventories; paths and inventories are infrastructure-sensitive.
- `summary`: fleet size, measurement and drift totals, and sweep timestamp; aggregate fleet metadata but no direct credential field.
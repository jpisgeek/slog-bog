# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: a56a7d4547d0c8ebbba46a25a46e39d1a0694289901101af0fcbbfa772ff34b6
reviewer: GPT-5 Codex   date: 2026-08-27
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:656-787,1430-1505 | Remote-controlled values can persist credentials in resource data, names, and tags. The credential denylist is incomplete and is not applied to tool names, config paths, or declared tools. | `const path = str(c?.path)` and `tool: printableRemoteText(tool)` | Strictly validate each field. Reject all URL userinfo and sensitive query/fragment data, including encoded forms, and never place rejected raw values in data, names, tags, logs, or errors. |
| 2 | block | mise/mise.ts:645-785 | API responses are not validated with strict schemas. Unknown fields are accepted and unchecked casts remain. | `.loose()`, `return v as T`, `raw as Record<string, unknown>` | Define strict schemas for complete top-level responses and nested entries; remove loose schemas and casts. |
| 3 | block | mise/mise.ts:713-810,1240-1335 | Malformed or missing response entries are silently skipped or replaced with empty values, allowing incomplete probes to appear measured and healthy. | `if (!parsed.success) continue`, `: []`, `Object.hasOwn(outdated, r.tool)` | Make any malformed required entry fail that subcommand. Mark the node degraded, retain unknown/null results, and never convert missing outdated/config data into false or empty. |
| 4 | block | mise/mise.ts:476-557,1025-1070 | Untrusted stdout, stderr, JSON fields, and readable resource-name components have no size limits. A compromised host can exhaust memory or cause datastore writes to fail. | `stdout: "piped"`, `stderr: "piped"`, `await cmd.output()`, `${prefix}-${flat` | Stream output with a strict byte cap, reject oversized fields, and cap the readable portion of resource names while retaining the full digest. |
| 5 | block | mise/mise.ts:29-43 | The printable-text filter escapes only C0/C1 bytes. Unicode formatting controls such as bidirectional overrides remain able to hide or reorder resource and log content. | `code <= 0x1f || (code >= 0x7f && code <= 0x9f)` | Escape or reject all Unicode control and formatting characters, including bidi controls, before logging, naming, tagging, or writing data. |
| 6 | fix | mise/README.md:89-128; mise/mise.ts:1275-1292 | Documentation says every unreadable follow-up response marks the node degraded, but successful garbage or empty output from `trust --show` and `--version` is accepted. | `if (!trust.ok)` and `if (!ver.ok)` | Validate the plain-text response formats and mark unreadable output degraded, or narrow the README claim. |
| 7 | note | mise/README.md:56-64,130-164 | Stored host labels and filesystem paths can disclose usernames, project names, and fleet structure. The README explicitly warns users about this infrastructure exposure. | `Install and config paths embed the remote account's home directory and real project directory names` | Retain the warning and ensure datastore access is restricted appropriately. |

## Checked and clean
Section 1 — Checked all published code, comments, literals, prose, examples, manifest, and license; no embedded credentials or unaccepted operator identifiers found. Accepted publisher identity, scope, repository, and product names were not reported.
Section 2 — See finding 1. No configured argument directly carries a password, token, private key, or secret-store reference.
Section 3 — SSH is encrypted, strict host-key checking is forced, every process has a deadline and caller cancellation signal, and no TLS-verification or cleartext option exists.
Section 4 — See findings 1–6. Shell metacharacters are rejected for remotely interpolated paths, local execution uses argv, and resource identities use length-prefixed SHA-256 input.
Section 5 — Only the sanctioned `npm:zod@4` import exists; no undeclared dependency, dynamic remote import, fetched executable code, `eval`, or `new Function` was found.
Section 6 — See finding 6. Other documented arguments, methods, resources, names, and failure behavior match the code.
Section 7 — See findings 1 and 7. The README discloses the intended infrastructure data, but credential exclusion is not guaranteed.

## Data written by this extension
- `node`: host label, measurement and degradation state, failure code, transport, mise version, measured directory, counts, and drift. Host labels and directories are infrastructure-identifying.
- `tool`: host label, tool name, requested/resolved/latest versions, installation and source paths, status, and drift. Paths may contain usernames or project names; finding 1 permits possible credential-bearing values.
- `config`: host label, config path, trust state, and declared/effective tool lists. Paths and labels are infrastructure-identifying; finding 1 permits possible credential-bearing values.
- `summary`: fleet counts and sweep timestamp. This is operational metadata without direct personal fields.
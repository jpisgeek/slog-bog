# Secrets & Security Review
extension: mise            version: 2026.08.28.3
content-hash: d6e2c9171790d21b4af62b37d0c9c7365be0a4944d6f2a1b897b5b3a02e58661
reviewer: GPT-5 Codex   date: 2026-08-28
verdict: PASS

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | operator-decision | mise/README.md:38 | The opt-in `errorDetail` feature durably stores untrusted error prose. Its heuristic screening cannot guarantee that every credential is removed. | `host error text routinely carries credential-bearing URLs, tokens, private hostnames and home paths` | Remove `errorDetail` and retain only closed-set error codes. Keeping it means explicitly accepting possible secret and infrastructure disclosure in the datastore. |
| 2 | operator-decision | mise/README.md:191 | Ambient SSH `SendEnv` and `SetEnv` configuration can disclose local environment values to a target that accepts them. The implementation deliberately retains ambient SSH configuration to preserve identity and jump-host settings. | `SendEnv and SetEnv in the operator's own config can ship local environment to a target` | Use an isolated SSH configuration and explicitly supply required safe connection settings. Keeping ambient configuration means accepting that matching environment variables may be sent. |
| 3 | operator-decision | mise/README.md:153 | `misePath` may execute any operator-selected file whose basename is `mise`; the file's identity or integrity is not verified. | `a file called mise can be anything -- /tmp/mise passes` | Restrict executable locations or require an integrity/signature check. Keeping flexible paths means treating the configured executable as fully trusted. |
| 4 | operator-decision | mise/README.md:220 | The extension intentionally stores infrastructure-identifying inventory, including host labels, account-bearing paths, project names, and potentially private repository or internal-domain source paths. | `Install and config paths embed the remote account's home directory and real project directory names.` | Remove, redact, or hash these fields and readable identifiers. Keeping them means treating the datastore, resource names, tags, and associated logs as infrastructure-sensitive. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: no credentials, real infrastructure identifiers, operator paths, secret-store names, or hidden control bytes were found in the published files; accepted publisher and repository identities were excluded.
2. Credential handling: see findings 1 and 2; no secret-bearing schema arguments, credential URLs, explicit secret logging, or disk persistence were otherwise found.
3. Transport and network: see finding 2; SSH is encrypted, strict host-key checking is enforced, dangerous forwarding and command options are disabled, calls are bounded, and caller cancellation is distinguished from timeout.
4. Untrusted input: see finding 3; remote values are bounded, screened, made printable, structurally validated, and collision-resistant; unknown probe results are not reported as healthy.
5. Supply chain: only the sanctioned `npm:zod@4` runtime import is present; no dynamic imports, undeclared dependencies, fetched executable code, `eval`, or `new Function` were found.
6. Documentation matches code: method, arguments, defaults, resources, naming, failure semantics, and example configuration match the implementation.
7. Data written: see findings 1 and 4; the README explicitly discloses the infrastructure-sensitive fields and optional error-text risk.

## Data written by this extension
- `node`: host label, measurement/degradation state, failed probes, error codes, optional error detail, mise version, transport, measured directory, counts, and drift. Host labels, directories, and optional error detail are sensitive; error detail may contain an undetected secret.
- `tool`: node label, tool and version data, install path, source type/path, state, and drift. Install and source paths may expose account names, private repositories, internal domains, and project structure.
- `config`: node label, configuration path, trust state, and declared/effective tools. Paths and labels identify fleet and project structure.
- `summary`: aggregate fleet counts and sweep timestamp; no direct personal or credential fields.
- Resource names and tags additionally expose host labels, tool names, configuration-path slugs, transport, and state.
# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 6a22634c4add1cb86a97f56a76e1ef98725ff710f36ab55a4a37872a99b8ee42
reviewer: GPT-5   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:912,1050,1073 | Credential screening misses common cookie/session assignments and prefixed environment-variable names. Values such as `GITHUB_TOKEN=...`, `SESSION_ID=...`, and cookie fields can pass unchanged into resource fields or `errorDetail`. | The assignment lists contain `token`, `password`, and `credential`, but omit session/cookie names; the surrounding word boundaries also prevent matching prefixed names. | Reject session/cookie identifiers and namespaced credential keys in both screening paths, cover known credential formats, and add regression tests for every class in section 1. Prefer closed-set diagnostic codes over persisted free text. |
| 2 | block | mise/mise.ts:1191,2261 | Pruning logs a stored resource name after checking only printability. An older or injected record name containing a credential or infrastructure identifier is copied verbatim into logs. | `name: z.string().min(1).refine(...)` followed by `ctx.logger.info("pruned {name}", { name: rec.data.name })` | Do not log stored names. Use a constant message or a non-reversible digest; never rely on printability as credential screening. |
| 3 | operator-decision | mise/README.md:204 | `errorDetail` deliberately persists host-controlled error prose. Even after improving known patterns, heuristic screening cannot guarantee that an ordinary-looking secret, hostname, account name, or path is removed. | `Screening is a set of shape rules, not a proof` | Remove `errorDetail` or accept that enabling it can durably disclose secrets and infrastructure to every datastore reader. |
| 4 | operator-decision | mise/README.md:216 | The extension deliberately stores infrastructure inventory in resource data, names, and tags, including host labels, home/project paths, config paths, install paths, and private repository or internal-domain source paths. | `Install and config paths embed the remote account's home directory and real project directory names.` | Remove or irreversibly pseudonymize these fields, or explicitly accept that the datastore and associated logs are infrastructure-sensitive. |
| 5 | operator-decision | mise/README.md:161 | `misePath` lets configuration select any local or remote executable whose basename is `mise`; a malicious file executes with the collector's privileges. | `a file called mise can be anything -- /tmp/mise passes` | Remove configurable executable paths, restrict them to administrator-controlled locations, or verify the executable before invocation. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
Section 1 — No embedded credentials, private infrastructure identifiers, non-placeholder addresses, private paths, or hidden control bytes found; accepted publisher/license/repository identities were excluded.
Section 2 — See findings 1–3; no secret schema arguments, URL-userinfo transmission, query-string credential transmission, or disk persistence found otherwise.
Section 3 — SSH is encrypted, strict host-key checking and forwarding restrictions are enforced, calls have deadlines, and caller cancellation is honoured; no TLS-disable or cleartext option exists.
Section 4 — See findings 1 and 5; response fields are runtime-validated, shell-bearing values are constrained, identities are collision-resistant, and failed measurements remain unknown rather than healthy.
Section 5 — Only sanctioned `npm:zod@4` is imported; no undeclared dependency, dynamic remote import, `eval`, `new Function`, or fetched-code execution was found.
Section 6 — README type, version, method, arguments, resources, naming, failure semantics, and example configuration match the code.
Section 7 — See findings 3–4; the README explicitly discloses the infrastructure-sensitive data written.

## Data written by this extension
- `node`: host label, measurement/degradation state, failed probes, transport, error codes, optional host-controlled `errorDetail`, mise version, measured directory, counts, and drift. Host labels, directories, and `errorDetail` are sensitive.
- `tool`: host label, tool and version details, install path, source type/path, state, and drift. Paths and private source locations are infrastructure-sensitive; screening defects may admit credentials.
- `config`: host label, config path, trust state, and declared/effective tools. Host labels and config paths are infrastructure-sensitive.
- `summary`: aggregate counts and sweep timestamp; no direct personal fields, but it exposes fleet size, health, and operational timing.
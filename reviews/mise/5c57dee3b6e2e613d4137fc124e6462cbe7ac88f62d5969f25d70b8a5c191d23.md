# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 5c57dee3b6e2e613d4137fc124e6462cbe7ac88f62d5969f25d70b8a5c191d23
reviewer: GPT-5   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:584 | SSH inherits `SendEnv` and `SetEnv` from ambient SSH configuration, potentially transmitting local secrets to every target. | `ssh inherits ambient config` | Use an isolated SSH configuration and allowlisted process environment; explicitly suppress environment forwarding. |
| 2 | block | mise/mise.ts:582 | SSH multiplexing is not disabled, so an existing control connection established under weaker host-key policy can bypass this invocation's strict verification. | `"StrictHostKeyChecking=yes"` without disabling `ControlPath` reuse | Disable connection sharing with `-S none` or equivalent locked-down `ControlMaster` and `ControlPath` options. |
| 3 | block | mise/mise.ts:1948,2253 | The fleet summary can report `expected: 0` when an installed, active tool has no resolved version and its expectation was therefore never evaluated. | `r.resolvedVersion !== null` and `expected: num(toolsComplete, count("expected"))` | Track expectation completeness and write `expected: null` whenever a relevant resolved version is unavailable. |
| 4 | block | mise/mise.ts:1626,1678,2291 | During the resource-name separator migration, a full sweep deletes legacy tool/config rows belonging to an unmeasured or degraded host, contradicting the documented retention guarantee. | `old single-dash names` cannot match the new double-dash prefix before `deleteResource` | Recognize and hold legacy prefixes for protected hosts during migration, or migrate names before pruning. |
| 5 | operator-decision | mise/mise.ts:396 | Opt-in `errorDetail` durably stores remote free text using heuristic screening that cannot guarantee removal of credentials, private hostnames, or local paths. | `host error text routinely carries credential-bearing URLs, tokens, private hostnames and home paths` | Remove free-text storage and retain only closed-set codes. Keeping it accepts possible durable secret and infrastructure disclosure. |
| 6 | operator-decision | mise/README.md:216 | Core resources, names, and tags deliberately retain fleet-identifying labels, directories, install paths, config paths, and source locations. | `Treat the datastore, and the sweep logs alongside it, as infrastructure-sensitive.` | Remove, hash, or redact these fields and readable identifiers. Keeping them accepts an infrastructure-sensitive datastore. |
| 7 | operator-decision | mise/README.md:132 | Fleet expectations deliberately cannot detect a required tool absent from a host's config, allowing `expected: 0` to coexist with an unverified requirement. | `its absence is invisible in the drift counts` | Materialize and evaluate every expected tool per host. Keeping the current design accepts that zero is not proof of compliance. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
Section 1 — No embedded credential, operator hostname, account ID, non-example address/domain, secret-store item, operator filesystem path, or hidden control byte found; see finding 6 for runtime data.
Section 2 — Secret arguments, logs, errors, persistence, and URL credentials checked; see findings 1 and 5.
Section 3 — Transport, host verification, schemes, timeouts, and cancellation checked; see findings 1 and 2.
Section 4 — Response validation, command construction, identity collisions, and false-health paths checked; see findings 3 and 7.
Section 5 — Only sanctioned `npm:zod@4` is imported; no dynamic imports, undeclared dependencies, evaluation, or fetched executable code found.
Section 6 — Manifest, model, method, arguments, resources, naming, examples, and failure behavior compared; see finding 4.
Section 7 — All resource fields, names, and tags reviewed; see findings 5–7.

## Data written by this extension
- `node`: host label, measured directory, transport, mise version, failure state, counts, drift, and optionally sensitive remote error text.
- `tool`: host/tool identity, requested/resolved/latest versions, install path, source type/path, state, and drift; paths and sources may identify accounts, projects, private repositories, or internal domains.
- `config`: host label, config path, trust state, declared/effective tool inventories, and degraded state.
- `summary`: fleet size, health/drift totals, degraded counts, and sweep timestamp.
- Resource names and tags additionally expose host labels, tool names, normalized config paths, transport, and health/drift states.
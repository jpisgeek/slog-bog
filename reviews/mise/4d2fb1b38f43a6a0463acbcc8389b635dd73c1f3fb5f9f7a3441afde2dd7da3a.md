# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 4d2fb1b38f43a6a0463acbcc8389b635dd73c1f3fb5f9f7a3441afde2dd7da3a
reviewer: GPT-5 Codex   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/README.md:38; mise/mise.ts:safeDetail | `errorDetail` can persist credentials from arbitrary host error text. Its screening is bypassable: whitespace separates `Bearer` from its token and PEM-header words from one another, while multi-word passwords are only partly withheld. | `host error text routinely carries credential-bearing URLs, tokens, private hostnames and home paths` | Remove `errorDetail` and never persist host-provided error text. Store only closed-set error codes. |
| 2 | block | mise/mise.ts:safeRemoteString | Arbitrary remote strings are protected by an incomplete secret-pattern blacklist. Credentials shaped like ordinary paths, URL path components, all-uppercase access keys, or unrecognised token formats can enter resource data, names, tags, and prune logs. | `a secret shaped like an ordinary path will pass` | Replace blacklist screening with strict field-specific allowlists. Do not store free-form source, install, config, or error paths that cannot be proven secret-free. |
| 3 | block | mise/mise.ts:LsEntrySchema | Remote API responses are explicitly accepted with unknown fields, contrary to the required strict-schema boundary. This affects tool, config, outdated, and stored-record schemas. | `.loose()` | Use strict schemas and classify unknown or malformed responses as incomplete measurements. |
| 4 | block | mise/mise.ts:SummarySchema | A wholly unmeasured host can still produce fleet totals of zero for tools, not-installed, not-active, and expected drift. Those zeros can present an incomplete sweep as healthy. | `notinstalled: count("notinstalled")` | Make every total dependent on successful tool discovery nullable, and write null whenever any contributing host or entry was not measured. |
| 5 | block | mise/mise.ts:MISE_BASENAME; mise/mise.ts:runMise | `misePath` selects an executable using only its filename. A path such as `/tmp/mise` passes validation and is executed, so the documented claim that the extension cannot change a host is not enforceable. | `MISE_BASENAME.test(v)` | Remove arbitrary executable selection or enforce a trusted executable-path policy. Document executable trust accurately and remove the unconditional read-only guarantee. |

## Checked and clean
1. Checked all published code, comments, examples, README, manifest, and LICENSE for embedded secrets and undisclosed operator identifiers; none found.
2. See findings 1–2.
3. Checked SSH encryption, strict host-key checking, forwarding controls, process deadlines, and caller cancellation; no additional transport finding.
4. See findings 3–5; shell-interpolated directory and binary strings otherwise reject shell metacharacters.
5. Checked imports, dependency declarations, dynamic loading, and code execution primitives; only sanctioned `npm:zod@4` is imported.
6. See finding 5; method count, arguments, resource fields, version, and failure descriptions otherwise match.
7. See findings 1–2; the README does disclose that written paths, labels, and repository locations are infrastructure-sensitive.

## Data written by this extension
- `node`: host label, measurement state, failure codes, transport, mise version, measured directory, counts, drift, and optionally secret-bearing `errorDetail`.
- `tool`: host and tool names, requested/resolved/latest versions, installation and source paths, state, and drift. Paths can identify accounts, projects, private repositories, or contain secrets.
- `config`: host label, configuration path, trust state, and declared/effective tool inventories. Paths and inventories are identifying infrastructure data.
- `summary`: fleet counts and sweep timestamp; no direct personal fields.
- Resource names and tags additionally expose host labels, tool names, transport, drift state, and normalized configuration-path material.
# Secrets & Security Review
extension: mise            version: 2026.08.24.1
content-hash: 65276e77989bb473b991bf596e5cf3de5ee6323ea9ed3d7719888a8641a236a9
reviewer: GPT-5   date: 2026-08-28
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | mise/mise.ts:942-977 | Malformed URL-shaped values fail open. A value such as `https://user:pass@` cannot be parsed, then bypasses the remaining checks and can be persisted in resource fields or `errorDetail`. This also contradicts the README’s claim that credential-bearing URLs are withheld. | `catch { return false; }` | Treat every unparseable `URLISH_RE` match as unsafe, and reject it rather than continuing. Add tests for malformed userinfo, query, fragment, cookie, and session-token forms. |
| 2 | operator-decision | mise/mise.ts:368-386 | The opt-in `errorDetail` feature durably stores attacker-controlled error prose. Heuristic screening cannot guarantee removal of cookies, session IDs, opaque credentials, private hostnames, or identifying paths. The risk and diagnostic rationale are explicitly documented. | `error text routinely carries credential-bearing URLs, tokens, private hostnames and home paths` | Decide whether to accept durable disclosure risk. For a guarantee, remove free-text persistence and retain only closed-set error codes or strictly structured allowlisted fields. |
| 3 | operator-decision | mise/mise.ts:1093-1148 | Responses are deliberately accepted with unknown fields through `.loose()`, contrary to the required strict-response policy. Known consumed fields are validated, but contract drift is not rejected. | `Unknown fields are tolerated on purpose` | Use strict schemas and explicitly version or adapt supported response shapes; keeping `.loose()` accepts the stated forward-compatibility trade-off. |
| 4 | note | mise/README.md:206-222 | Resources, names, and tags intentionally expose infrastructure-sensitive inventory including host labels, account-bearing paths, project paths, and private source locations. The README clearly discloses this and tells users to protect the datastore and logs. | `Treat the datastore, and the sweep logs alongside it, as infrastructure-sensitive.` | No code change required for the documented feature; retain the warning and restrict access to the datastore and logs. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Published surface: checked source, comments, examples, manifest, README and LICENSE; no real credentials, operator infrastructure, private addresses, secret-store names, local identity paths, or hidden control bytes found. Accepted publisher and repository identities were not reported.
2. Credential handling: see findings 1–2; no secret-valued configuration argument or direct disk persistence was found.
3. Transport and network: SSH is encrypted, strict host-key checking is forced, forwarding is disabled, process deadlines exist, and caller cancellation is preserved. No cleartext URL mode, TLS-disable option, redirect handling, or direct HTTP client exists.
4. Untrusted input: see finding 3; shell-bound paths are constrained, local execution uses argv, output is bounded, controls are escaped, failures remain distinguishable from health, and resource identities use collision-resistant hashes.
5. Supply chain: only sanctioned `npm:zod@4` is imported; no undeclared, dynamically loaded, fetched-and-executed, or evaluated code was found.
6. Documentation: see finding 1; otherwise model version, arguments, method count, resource fields, naming, failure behavior, deletion behavior, and examples match the code.
7. Data written: see findings 1–2 and 4; the README identifies the infrastructure-sensitive fields and storage surfaces.

## Data written by this extension
- `node`: host label, measured/degraded state, failed probes, failure codes, transport, mise version, measured directory, counts and drift. Optional `errorDetail` may contain credentials, private hostnames, account names, or paths.
- `tool`: host and tool names, requested/resolved/latest versions, install path, source type/path, status and drift. Paths and source locations may identify accounts, projects, private repositories, or internal domains.
- `config`: host label, config path, trust state and declared/effective tool names. Paths may expose home directories, account names, and projects.
- `summary`: aggregate fleet counts and sweep timestamp; no direct credential field.
- Resource names and tags additionally expose host labels, tool names, config paths, transport, and health/drift state.
# Secrets & Security Review
extension: @jpisgeek/anthropic-usage            version: 2026.09.05.1
content-hash: 49ff424bfe44acc98791dbef379e06140b0ab86af5752ec39cf89610300111c1
reviewer: gpt-daybreak-blue-latest   date: 2026-09-05
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | `anthropic-usage/dashboard_anthropic.ts:1042` | The dashboard bundle ID is constant across every Anthropic usage model instance, so distinct Platform or Enterprise instances collide and a consumer keyed by bundle ID can silently replace one organization's observation with another. | `id: "anthropic-organization"` is emitted even though `ctx.modelId` identifies the producing instance and is already carried in `producer.modelId`. | Derive a stable, collision-resistant bundle ID from the model instance identity, using domain separation and a full cryptographic digest if the raw identifier should not appear in the ID. Add a regression proving two model IDs produce different bundle IDs and one model ID remains stable. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: reviewed all five published files, including comments, literals, examples, and prose; found no credential value, private hostname, account identifier, local path, control character, or non-vendor domain. The publisher name, public package scope, repository, and Anthropic product/API names are accepted public identities.
2. Credential handling: the organization credential is required and marked sensitive, placed only in the selected fixed request header, and absent from URLs, locally generated error messages, resources, tags, and reports. Response bodies and server error text are not persisted. The documented vault names are explicit placeholders.
3. Transport and network: requests use the fixed official Anthropic HTTPS origin and fixed product-specific paths, expose no URL override, reject redirects, apply per-request and five-minute walk bounds, and combine timeouts with caller cancellation while preserving caller aborts. Authentication type is constrained by account kind.
4. Untrusted input: endpoint- and account-specific page, bucket, and row schemas are strict; requested grouping fields are required; bucket times, order, overlap, and maximum width are checked; pagination is bounded and rejects missing/repeated cursors; aggregate overflow and malformed snapshots degrade explicitly instead of becoming healthy zeroes. Exact decimal cost totals are reconciled against breakdowns without floating-point arithmetic. No input reaches HTML, shell, SQL, or filesystem paths. Identity collision: see finding 1.
5. Supply chain: the only dependency is the sanctioned runtime import `npm:zod@4`; there are no dynamic imports, fetched code, evaluation, or deserialization into code. The inlined dashboard contract is byte-identical to the previously reviewed canonical copy.
6. Documentation matches code: verified the model type and version, arguments, single `collect` method, `snapshot` resource and 90-day lifetime, account/authentication boundary, two independent paginated walks, strict validation and partial states, top-100 warning, per-dimension refresh evidence, exact fractional-minor-unit costs, examples, and security statements against the implementation.
7. Data written: see below. Stored workspace IDs, models, products, descriptions, usage, authoritative cost, and producer model identity can expose operational or internal workload information; the README explicitly warns users and the report sensitivity metadata enumerates these fields. No credential is written.

## Data written by this extension
- `snapshot` resource, retained for 90 days: fixed provider, account kind, collection and coverage times, independent usage/cost refresh evidence and status, aggregate token/request counters, workspace/model/product usage breakdowns, exact decimal minor-unit currency totals, and workspace/model/product/description cost breakdowns. It contains operational and potentially internal workload metadata, but no organization credential, API-key ID, organization ID, tagged user ID, RBAC group ID, or Slack channel ID.
- Report JSON: dashboard bundle containing aggregate usage and authoritative costs plus the local Swamp producer model name and model ID, with workspace, model, product, description, and producer identity fields listed in sensitivity metadata.
- Report Markdown: bundle title and state plus aggregate token and exact minor-unit cost section summaries; it contains no credential or workspace/model/description breakdown identifiers.

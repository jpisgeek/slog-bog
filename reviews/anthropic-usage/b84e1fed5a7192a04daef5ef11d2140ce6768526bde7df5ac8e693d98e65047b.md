# Secrets & Security Review
extension: anthropic-usage            version: 2026.08.25.2
content-hash: b84e1fed5a7192a04daef5ef11d2140ce6768526bde7df5ac8e693d98e65047b
reviewer: GPT-5   date: 2026-08-30
verdict: FAIL

## Findings
| # | severity | file:line | what | evidence | fix |
|---|----------|-----------|------|----------|-----|
| 1 | block | README.md:54 | The example exposes a plausible secret-store item name rather than an obviously fake placeholder. | `vault.get("example-vault", "ANTHROPIC_ADMIN_KEY")` | Replace the item name with an unmistakable placeholder such as `YOUR_ANTHROPIC_ADMIN_KEY`. |
| 2 | block | anthropic_usage.ts:260-295,420-456 | API responses are not validated with strict, endpoint-specific schemas. A missing or mistyped pagination flag is treated as successful completion, while missing or wrongly typed grouping dimensions are silently converted to null. This can report truncated or malformed data as complete and healthy. | `if (object.has_more !== true)` and `typeof item.model === "string" ? item.model : null` | Parse every page and row through strict endpoint-specific schemas. Reject missing or mistyped pagination and requested grouping fields, and return partial or invalid-response status instead of complete. |
| 3 | block | anthropic_usage.ts:180-184,279-281; dashboard_anthropic.ts:578-586,682-690 | A present but invalid vendor refresh timestamp is silently discarded, after which the dashboard substitutes collection time and labels freshness as fresh. Malformed freshness evidence can therefore become healthy state. | `if (refreshed) refreshedAt = refreshed` and `observedAt: s.dataRefreshedAt ?? s.collectedAt` | Distinguish absent, valid, and invalid refresh timestamps. A present invalid value must produce invalid-response or unknown freshness, not a fresh observation. |
| 4 | block | dashboard_anthropic.ts:415-421,724-760; README.md:75-78 | Dashboard JSON persists the operator's Swamp model name and model ID, but those infrastructure identifiers are absent from the sensitivity field list and README disclosure. | `modelName: ctx.definition.name` and `modelId: ctx.modelId` | Add these producer fields to sensitivity metadata and explicitly disclose them in the README, or redact/omit them from exportable dashboard output. |
| 5 | operator-decision | anthropic_usage.ts:363-371,420-438 | Missing cache counters are deliberately converted to zero. This supports sparse vendor responses, but a vendor regression or schema change can undercount usage while retaining complete status. | `return value === undefined ? 0 : integer(value)` | The operator must explicitly accept this undercount risk or require the fields and classify omission as invalid-response. |

severity: block (must fix before publish) · fix (should fix; not a publish
blocker on its own) · note (informational) · operator-decision (trade-off the
operator must rule on)
a documented, deliberate design choice whose only fix is to give up the
feature is operator-decision, not block. See "Assigning severity" above.
verdict is FAIL if any finding is block, or if input was incomplete.

## Checked and clean
1. Secrets and identifying information: see findings 1 and 4; no actual credentials, private hosts, addresses, local paths, or unaccepted identities found.
2. Credential handling: credential is sensitive, header-only, sanitized from errors and output, and not persisted.
3. Transport and network: fixed official HTTPS origin, rejected redirects, per-request timeout, and caller cancellation are present; no TLS-disable or alternate-scheme option found.
4. Untrusted input: see findings 2, 3, and 5; no shell, SQL, path, or HTML interpolation sink found.
5. Supply chain: only sanctioned `npm:zod@4` is imported; no dynamic code loading, undeclared dependency, eval, or executable deserialization found.
6. Documentation matches code: see finding 4; documented arguments, method, resource, version, account behavior, cost units, and failure states otherwise match.
7. Data written: see finding 4; README discloses workload-breakdown sensitivity, and no credential is written.

## Data written by this extension
- `snapshot` / `organization-usage`: provider, account kind, collection and coverage timestamps, refresh timestamp, usage/cost status, token and request totals, currency totals, and grouped-cap flags.
- Snapshot breakdowns: workspace IDs and model, product, and description values. These are operationally sensitive and can identify internal workloads; no credential is included.
- Dashboard JSON and Markdown: aggregate metrics, coverage, freshness, completeness, exceptions, account kind, and status.
- Dashboard producer metadata: Swamp model type, model name, model ID, data name/version, extension version, and report name. Model name and ID identify the operator's local infrastructure.
# swamp runtime state never belongs in this repo
.swamp/
# review scratch
reviews/**/*.tmp
# editor / OS
.DS_Store
*.swp

# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml
# END swamp managed section
# deno.lock does not govern extension deps (swamp bundles them); keep it out of the repo
deno.lock

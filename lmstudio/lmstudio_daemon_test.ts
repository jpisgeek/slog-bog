/**
 * Sibling test entry for the daemon model.
 *
 * The package's shared suite keeps cross-model invariants together in
 * lmstudio_test.ts; importing it here also makes the daemon's tests discoverable
 * by Swamp's per-entrypoint review check.
 */
import "./lmstudio_test.ts";
